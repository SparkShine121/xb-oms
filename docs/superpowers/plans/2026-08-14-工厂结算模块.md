# 工厂结算模块（模块 4）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付工厂结算单（FactoryPayment）+ 付款记录（FactoryPaymentRecord）+ 一键生成 + 工厂对账单 + 订单详情结算状态（PC + 移动端完整）。

**Architecture:** 新建 `apps/factory_payment`（FactoryPayment OneToOne→OrderItem + FactoryPaymentRecord 多次付款 + ViewSet + 权限 + 一键生成 + 对账汇总）；OrderItem.factory_payment_id 删除改 OneToOne（migration）；前端 `views/factory_payment`（PC）+ `views/m/factory_payment`（移动端完整）。沿用前三模块基础设施。

**Tech Stack:** Python 3.13 + Django 5.1 + DRF + SQLite + pytest；Vue3 + Vite + TS + Element Plus + Vant + Pinia + axios + Vitest。implementer 用 sonnet，reviewer 用 fable。

## Global Constraints

- 工作目录 `D:\Pad Windows Data\Claude项目\xb项目`。Python `D:\Pad Windows Data\Python313-arm64\python.exe`，`python -m pip`，装依赖经 `backend/requirements/dev.txt`（不裸装）。
- Django 5.1+，后端测试 pytest。Node `D:\Pad Windows Data\nodejs\node-v22.23.2-win-arm64\`（用前 `export PATH="/d/Pad Windows Data/nodejs/node-v22.23.2-win-arm64:$PATH"`），npm `--registry=https://registry.npmmirror.com`。
- 提交信息尾行 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。中文路径 git 已设 `core.quotepath false`。
- 沿用前三模块：`common.views.BaseModelViewSet`（统一 `{code,message,data}` 包装）、`common.response`（success_response/error_response）、`common.permissions`、django-filter 已配。
- 测试复用 `apps/orders/tests/conftest.py` autouse `create_groups` 模式：factory_payment 测试需自己的 conftest。
- 前端测试首行 `// @vitest-environment node` + `vi.mock('element-plus', ...)`；vitest ARM 用 `--test-timeout=30000`。
- 后端 8000、前端 5173、Vite 代理 /api。

## File Structure

**后端：**
- `backend/apps/factory_payment/{__init__,apps,models,serializers,views,permissions,urls,admin}.py`
- `backend/apps/factory_payment/migrations/`
- `backend/apps/factory_payment/tests/{conftest,test_models,test_api,test_permission}.py`
- `backend/apps/orders/models.py`（删除 OrderItem.factory_payment_id，migration）
- `backend/xb_project/settings/base.py`（INSTALLED_APPS 加 apps.factory_payment）
- `backend/xb_project/urls.py`（挂载 /api/factory-payment/）

**前端：**
- `frontend/src/api/factoryPayment.ts`
- `frontend/src/views/factory_payment/{FactoryPaymentList,FactoryPaymentDetail,FactoryStatement}.vue`
- `frontend/src/views/m/factory_payment/{MFactoryPaymentList,MFactoryPaymentDetail,MFactoryStatement}.vue`
- `frontend/src/views/orders/OrderDetail.vue`（加结算状态区块）
- `frontend/src/router/index.ts`（加路由）
- `frontend/src/layouts/{MainLayout,MobileLayout}.vue`（加菜单/tabbar）
- `frontend/src/tests/factoryPayment.test.ts`

---

## Task 1: FactoryPayment/FactoryPaymentRecord 模型 + migration

**Files:**
- Create: `backend/apps/factory_payment/{__init__,apps,models}.py`
- Modify: `backend/apps/orders/models.py`（删除 factory_payment_id）
- Modify: `backend/xb_project/settings/base.py`（INSTALLED_APPS）
- Test: `backend/apps/factory_payment/tests/{conftest,test_models}.py`

**Interfaces:**
- Produces: `FactoryPayment`（order_item OneToOne + factory FK + amount_cny + paid_amount + status + note + 时间戳，save 自动算 status）、`FactoryPaymentRecord`（factory_payment FK + amount + payment_date + note + 时间戳）

- [ ] **Step 1: 建 factory_payment app 包**

```bash
cd backend
python manage.py startapp factory_payment apps/factory_payment
```
在 `xb_project/settings/base.py` 的 INSTALLED_APPS 加 `'apps.factory_payment'`。`apps.py` 的 `name` 改为 `'apps.factory_payment'`。删除默认 `tests.py`（用 tests/ 目录）。

- [ ] **Step 2: 写失败测试**

`backend/apps/factory_payment/tests/conftest.py`：
```python
import pytest
from django.core.management import call_command

@pytest.fixture(autouse=True)
def ensure_groups(db):
    call_command('create_groups')
```

`backend/apps/factory_payment/tests/test_models.py`：
```python
import pytest
from apps.factory_payment.models import FactoryPayment, FactoryPaymentRecord
from apps.orders.models import Order, OrderItem
from apps.basic_info.models import Factory, Customer
from django.contrib.auth.models import User

@pytest.fixture
def setup(db):
    f = Factory.objects.create(name='华鑫')
    u = User.objects.create_user('admin1', password='pw123456')
    c = Customer.objects.create(name='客户A', salesman=None)
    o = Order.objects.create(order_no='O1', tracking_status='排产', customer=c, amount_usd='100')
    item = OrderItem.objects.create(order=o, seq=1, factory=f, qty=10, unit_price='10', subtotal='100', cost_price='7.20')
    return {'factory': f, 'order': o, 'item': item, 'user': u}

def test_factory_payment_status_auto(setup):
    s = setup
    fp = FactoryPayment.objects.create(
        order_item=s['item'], factory=s['factory'], amount_cny='72.00'
    )
    assert fp.status == '未结'
    assert fp.paid_amount == 0

    FactoryPaymentRecord.objects.create(
        factory_payment=fp, amount='30.00', payment_date='2026-08-14'
    )
    fp.refresh_from_db()
    assert fp.paid_amount == 30
    assert fp.status == '部分结'

    FactoryPaymentRecord.objects.create(
        factory_payment=fp, amount='42.00', payment_date='2026-08-15'
    )
    fp.refresh_from_db()
    assert fp.paid_amount == 72
    assert fp.status == '已结'

def test_factory_payment_onetoone(setup):
    s = setup
    FactoryPayment.objects.create(
        order_item=s['item'], factory=s['factory'], amount_cny='72.00'
    )
    with pytest.raises(Exception):
        FactoryPayment.objects.create(
            order_item=s['item'], factory=s['factory'], amount_cny='72.00'
        )
```

- [ ] **Step 3: 运行验证失败**

Run: `cd backend && python -m pytest apps/factory_payment/tests/ -v`
Expected: FAIL（apps.factory_payment 不存在）

- [ ] **Step 4: 实现 models.py**

```python
from django.db import models
from apps.orders.models import OrderItem
from apps.basic_info.models import Factory

class FactoryPayment(models.Model):
    order_item = models.OneToOneField(
        OrderItem, on_delete=models.CASCADE, related_name='factory_payment'
    )
    factory = models.ForeignKey(
        Factory, on_delete=models.CASCADE, related_name='factory_payments'
    )
    amount_cny = models.DecimalField(max_digits=14, decimal_places=2)
    paid_amount = models.DecimalField(max_digits=14, decimal_places=2, default=0)
    status = models.CharField(max_length=16, default='未结')
    note = models.TextField(blank=True)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    def save(self, *args, **kwargs):
        if self.paid_amount >= self.amount_cny and self.amount_cny > 0:
            self.status = '已结'
        elif self.paid_amount > 0:
            self.status = '部分结'
        else:
            self.status = '未结'
        super().save(*args, **kwargs)

class FactoryPaymentRecord(models.Model):
    factory_payment = models.ForeignKey(
        FactoryPayment, on_delete=models.CASCADE, related_name='records'
    )
    amount = models.DecimalField(max_digits=14, decimal_places=2)
    payment_date = models.DateField()
    note = models.TextField(blank=True)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
```

- [ ] **Step 5: 删除 OrderItem.factory_payment_id + makemigrations + migrate**

`backend/apps/orders/models.py`：删除 `factory_payment_id = models.IntegerField(null=True, blank=True)` 行。

```bash
python manage.py makemigrations factory_payment orders
python manage.py migrate
```

- [ ] **Step 6: 运行测试通过**

Run: `cd backend && python -m pytest apps/factory_payment/tests/ -v && python -m pytest -v`
Expected: factory_payment 2 passed + 全量 passed（含前三模块无回归）

- [ ] **Step 7: Commit**

```bash
git add backend/apps/factory_payment backend/apps/orders/models.py backend/xb_project
git commit -m "feat(factory_payment): FactoryPayment/Record 模型 + migration

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 2: FactoryPaymentSerializer + FactoryPaymentRecordSerializer

**Files:**
- Create: `backend/apps/factory_payment/serializers.py`
- Test: `backend/apps/factory_payment/tests/test_serializer.py`

**Interfaces:**
- Produces: `FactoryPaymentSerializer`（嵌套 order_item_info + records）、`FactoryPaymentRecordSerializer`

- [ ] **Step 1: 写失败测试**

`backend/apps/factory_payment/tests/test_serializer.py`：
```python
import pytest
from apps.factory_payment.serializers import FactoryPaymentSerializer, FactoryPaymentRecordSerializer
from apps.factory_payment.models import FactoryPayment, FactoryPaymentRecord
from apps.orders.models import Order, OrderItem
from apps.basic_info.models import Factory, Customer

@pytest.fixture
def setup(db):
    f = Factory.objects.create(name='华鑫')
    c = Customer.objects.create(name='客户A')
    o = Order.objects.create(order_no='O1', tracking_status='排产', customer=c, amount_usd='100')
    item = OrderItem.objects.create(order=o, seq=1, factory=f, qty=10, unit_price='10', subtotal='100', cost_price='7.20')
    fp = FactoryPayment.objects.create(order_item=item, factory=f, amount_cny='72.00')
    return {'fp': fp, 'item': item, 'factory': f}

def test_serializer_fields(setup):
    s = FactoryPaymentSerializer(setup['fp'])
    assert s.data['amount_cny'] == '72.00'
    assert s.data['paid_amount'] == '0.00'
    assert s.data['status'] == '未结'
    assert s.data['factory_name'] == '华鑫'
    assert s.data['order_no'] == 'O1'
    assert isinstance(s.data['records'], list) and len(s.data['records']) == 0

def test_record_serializer(setup):
    rec = FactoryPaymentRecord.objects.create(
        factory_payment=setup['fp'], amount='30.00', payment_date='2026-08-14'
    )
    s = FactoryPaymentRecordSerializer(rec)
    assert s.data['amount'] == '30.00'
```

- [ ] **Step 2: 运行验证失败**

Run: `cd backend && python -m pytest apps/factory_payment/tests/test_serializer.py -v`
Expected: FAIL

- [ ] **Step 3: 实现 serializers.py**

```python
from rest_framework import serializers
from .models import FactoryPayment, FactoryPaymentRecord

class FactoryPaymentRecordSerializer(serializers.ModelSerializer):
    class Meta:
        model = FactoryPaymentRecord
        fields = ['id', 'factory_payment', 'amount', 'payment_date', 'note', 'created_at', 'updated_at']
        read_only_fields = ['created_at', 'updated_at']

class FactoryPaymentSerializer(serializers.ModelSerializer):
    records = FactoryPaymentRecordSerializer(many=True, read_only=True)
    factory_name = serializers.CharField(source='factory.name', read_only=True)
    order_no = serializers.CharField(source='order_item.order.order_no', read_only=True)
    product_no = serializers.CharField(source='order_item.product_no', read_only=True, default='')

    class Meta:
        model = FactoryPayment
        fields = ['id', 'order_item', 'factory', 'factory_name', 'order_no', 'product_no',
                  'amount_cny', 'paid_amount', 'status', 'note', 'records',
                  'created_at', 'updated_at']
        read_only_fields = ['paid_amount', 'status', 'created_at', 'updated_at']
```

- [ ] **Step 4: 运行测试通过**

Run: `cd backend && python -m pytest apps/factory_payment/tests/ -v`
Expected: PASS（4 tests）

- [ ] **Step 5: Commit**

```bash
git add backend/apps/factory_payment/serializers.py backend/apps/factory_payment/tests/test_serializer.py
git commit -m "feat(factory_payment): 序列化器

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 3: FactoryPaymentPermission + ViewSet + API + urls + 一键生成 + 对账

**Files:**
- Create: `backend/apps/factory_payment/{permissions,views,urls}.py`
- Modify: `backend/xb_project/urls.py`
- Test: `backend/apps/factory_payment/tests/{test_api,test_permission}.py`

**Interfaces:**
- Produces: `FactoryPaymentViewSet`（CRUD + generate + statement）、`FactoryPaymentRecordViewSet`（CRUD）、`FactoryPaymentPermission`

- [ ] **Step 1: 写失败测试**

`backend/apps/factory_payment/tests/test_permission.py`：
```python
import pytest
from django.contrib.auth.models import User, Group
from rest_framework.test import APIClient
from apps.orders.models import Order, OrderItem
from apps.basic_info.models import Factory, Customer

@pytest.fixture
def admin_client(db):
    u = User.objects.create_user('admin1', password='pw123456')
    u.groups.add(Group.objects.get(name='admin'))
    c = APIClient(); c.force_authenticate(u); return c

@pytest.fixture
def finance_client(db):
    u = User.objects.create_user('fin1', password='pw123456')
    u.groups.add(Group.objects.get(name='finance'))
    c = APIClient(); c.force_authenticate(u); return c

@pytest.fixture
def salesman_client(db):
    u = User.objects.create_user('sales1', password='pw123456')
    u.groups.add(Group.objects.get(name='salesman'))
    c = APIClient(); c.force_authenticate(u); return c

def test_admin_can_create(db, admin_client):
    f = Factory.objects.create(name='华鑫')
    c = Customer.objects.create(name='客户A')
    o = Order.objects.create(order_no='O1', tracking_status='排产', customer=c, amount_usd='100')
    item = OrderItem.objects.create(order=o, seq=1, factory=f, qty=10, unit_price='10', subtotal='100', cost_price='7.20')
    r = admin_client.post('/api/factory-payment/payments/', {
        'order_item': item.id, 'factory': f.id, 'amount_cny': '72.00'
    }, format='json')
    assert r.status_code == 201

def test_finance_can_create(db, finance_client):
    f = Factory.objects.create(name='华鑫')
    c = Customer.objects.create(name='客户A')
    o = Order.objects.create(order_no='O1', tracking_status='排产', customer=c, amount_usd='100')
    item = OrderItem.objects.create(order=o, seq=1, factory=f, qty=10, unit_price='10', subtotal='100', cost_price='7.20')
    r = finance_client.post('/api/factory-payment/payments/', {
        'order_item': item.id, 'factory': f.id, 'amount_cny': '72.00'
    }, format='json')
    assert r.status_code == 201

def test_salesman_cannot_create(db, salesman_client):
    f = Factory.objects.create(name='华鑫')
    c = Customer.objects.create(name='客户A')
    o = Order.objects.create(order_no='O1', tracking_status='排产', customer=c, amount_usd='100')
    item = OrderItem.objects.create(order=o, seq=1, factory=f, qty=10, unit_price='10', subtotal='100', cost_price='7.20')
    r = salesman_client.post('/api/factory-payment/payments/', {
        'order_item': item.id, 'factory': f.id, 'amount_cny': '72.00'
    }, format='json')
    assert r.status_code == 403
```

`backend/apps/factory_payment/tests/test_api.py`：
```python
import pytest
from django.contrib.auth.models import User, Group
from rest_framework.test import APIClient
from apps.orders.models import Order, OrderItem
from apps.basic_info.models import Factory, Customer
from apps.factory_payment.models import FactoryPayment, FactoryPaymentRecord

@pytest.fixture
def admin_client(db):
    u = User.objects.create_user('admin1', password='pw123456')
    u.groups.add(Group.objects.get(name='admin'))
    c = APIClient(); c.force_authenticate(u); return c

def test_generate_by_order(db, admin_client):
    f = Factory.objects.create(name='华鑫')
    c = Customer.objects.create(name='客户A')
    o = Order.objects.create(order_no='O1', tracking_status='排产', customer=c, amount_usd='100')
    OrderItem.objects.create(order=o, seq=1, factory=f, qty=10, unit_price='10', subtotal='100', cost_price='7.20')
    r = admin_client.post(f'/api/factory-payment/orders/{o.id}/generate/', format='json')
    assert r.status_code == 200
    assert FactoryPayment.objects.count() == 1
    fp = FactoryPayment.objects.first()
    assert str(fp.amount_cny) == '72.00'

def test_generate_skips_existing(db, admin_client):
    f = Factory.objects.create(name='华鑫')
    c = Customer.objects.create(name='客户A')
    o = Order.objects.create(order_no='O1', tracking_status='排产', customer=c, amount_usd='100')
    item = OrderItem.objects.create(order=o, seq=1, factory=f, qty=10, unit_price='10', subtotal='100', cost_price='7.20')
    FactoryPayment.objects.create(order_item=item, factory=f, amount_cny='72.00')
    r = admin_client.post(f'/api/factory-payment/orders/{o.id}/generate/', format='json')
    assert r.status_code == 200
    assert FactoryPayment.objects.count() == 1

def test_payment_record_updates_status(db, admin_client):
    f = Factory.objects.create(name='华鑫')
    c = Customer.objects.create(name='客户A')
    o = Order.objects.create(order_no='O1', tracking_status='排产', customer=c, amount_usd='100')
    item = OrderItem.objects.create(order=o, seq=1, factory=f, qty=10, unit_price='10', subtotal='100', cost_price='7.20')
    fp = FactoryPayment.objects.create(order_item=item, factory=f, amount_cny='72.00')
    r = admin_client.post('/api/factory-payment/records/', {
        'factory_payment': fp.id, 'amount': '30.00', 'payment_date': '2026-08-14'
    }, format='json')
    assert r.status_code == 201
    fp.refresh_from_db()
    assert str(fp.paid_amount) == '30.00'
    assert fp.status == '部分结'

def test_statement(db, admin_client):
    f = Factory.objects.create(name='华鑫')
    c = Customer.objects.create(name='客户A')
    o = Order.objects.create(order_no='O1', tracking_status='排产', customer=c, amount_usd='100')
    item = OrderItem.objects.create(order=o, seq=1, factory=f, qty=10, unit_price='10', subtotal='100', cost_price='7.20')
    FactoryPayment.objects.create(order_item=item, factory=f, amount_cny='72.00')
    r = admin_client.get('/api/factory-payment/statement/', {'factory': f.id})
    assert r.status_code == 200
    assert 'total_amount' in r.data
```

- [ ] **Step 2: 运行验证失败**

Run: `cd backend && python -m pytest apps/factory_payment/tests/ -v`
Expected: FAIL

- [ ] **Step 3: 实现 permissions.py**

```python
from rest_framework.permissions import BasePermission

class FactoryPaymentPermission(BasePermission):
    def has_permission(self, request, view):
        if not request.user or not request.user.is_authenticated:
            return False
        groups = set(request.user.groups.values_list('name', flat=True))
        if 'admin' in groups or 'finance' in groups:
            return True
        if view.action in ('create', 'update', 'partial_update', 'destroy'):
            return False
        return True
```

- [ ] **Step 4: 实现 views.py**

```python
from rest_framework import viewsets
from rest_framework.decorators import action
from rest_framework.permissions import IsAuthenticated
from django.db import transaction
from django.db.models import Q, Sum
from common.views import BaseModelViewSet
from common.response import success_response, error_response
from apps.orders.models import Order, OrderItem
from .models import FactoryPayment, FactoryPaymentRecord
from .serializers import FactoryPaymentSerializer, FactoryPaymentRecordSerializer
from .permissions import FactoryPaymentPermission

class FactoryPaymentViewSet(BaseModelViewSet):
    serializer_class = FactoryPaymentSerializer
    permission_classes = [IsAuthenticated, FactoryPaymentPermission]
    filterset_fields = ['factory', 'status']
    search_fields = ['order_item__order__order_no', 'factory__name']
    ordering_fields = ['id', 'created_at', 'amount_cny', 'paid_amount']
    ordering = ['-created_at']

    def get_queryset(self):
        u = self.request.user
        groups = set(u.groups.values_list('name', flat=True))
        qs = FactoryPayment.objects.select_related('order_item__order', 'factory')
        if 'admin' in groups or 'finance' in groups:
            return qs
        cond = Q()
        if 'salesman' in groups:
            cond |= Q(order_item__order__customer__salesman=u)
        if 'tracker' in groups:
            cond |= Q(order_item__order__tracker=u)
        return qs.filter(cond).distinct() if cond else qs.none()

    @action(detail=False, methods=['post'], url_path=r'orders/(?P<order_id>\d+)/generate')
    def generate_by_order(self, request, order_id=None):
        try:
            order = Order.objects.get(pk=order_id)
        except Order.DoesNotExist:
            return error_response(1004, '订单不存在', status=404)
        items = order.items.filter(factory__isnull=False)
        created, skipped = 0, 0
        for item in items:
            if hasattr(item, 'factory_payment'):
                skipped += 1
                continue
            FactoryPayment.objects.create(
                order_item=item, factory=item.factory,
                amount_cny=item.qty * item.cost_price
            )
            created += 1
        return success_response({'created_count': created, 'skipped_count': skipped})

    @action(detail=False, methods=['get'], url_path='statement')
    def statement(self, request):
        qs = self.get_queryset()
        factory_id = request.query_params.get('factory')
        if factory_id:
            qs = qs.filter(factory_id=factory_id)
        total_amount = qs.aggregate(t=Sum('amount_cny'))['t'] or 0
        total_paid = qs.aggregate(t=Sum('paid_amount'))['t'] or 0
        return success_response({
            'total_amount': str(total_amount),
            'total_paid': str(total_paid),
            'total_unpaid': str(total_amount - total_paid),
            'count': qs.count(),
        })

class FactoryPaymentRecordViewSet(BaseModelViewSet):
    serializer_class = FactoryPaymentRecordSerializer
    permission_classes = [IsAuthenticated, FactoryPaymentPermission]
    filterset_fields = ['factory_payment']
    ordering = ['-created_at']

    def get_queryset(self):
        return FactoryPaymentRecord.objects.select_related('factory_payment__order_item__order')

    def perform_create(self, serializer):
        with transaction.atomic():
            record = serializer.save()
            fp = record.factory_payment
            fp.paid_amount += record.amount
            fp.save()
```

- [ ] **Step 5: 实现 urls.py + 挂载**

```python
from rest_framework.routers import DefaultRouter
from .views import FactoryPaymentViewSet, FactoryPaymentRecordViewSet

router = DefaultRouter()
router.register('payments', FactoryPaymentViewSet, basename='factory-payment')
router.register('records', FactoryPaymentRecordViewSet, basename='factory-payment-record')
urlpatterns = router.urls
```

`xb_project/urls.py` 加 `path('api/factory-payment/', include('apps.factory_payment.urls'))`。

- [ ] **Step 6: 运行测试通过**

Run: `cd backend && python -m pytest apps/factory_payment/tests/ -v && python manage.py check`
Expected: PASS + 0 issues

- [ ] **Step 7: Commit**

```bash
git add backend/apps/factory_payment backend/xb_project
git commit -m "feat(factory_payment): ViewSet + 权限 + 一键生成 + 对账 API

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 4: 前端 api/factoryPayment.ts + 路由 + 菜单

**Files:**
- Create: `frontend/src/api/factoryPayment.ts`
- Modify: `frontend/src/router/index.ts`、`frontend/src/layouts/{MainLayout,MobileLayout}.vue`
- Test: `frontend/src/tests/factoryPayment.test.ts`

- [ ] **Step 1: 写失败测试**

`frontend/src/tests/factoryPayment.test.ts`（首行 `// @vitest-environment node`）：
```ts
import { vi } from 'vitest'
vi.mock('element-plus', () => ({ ElMessage: { error: () => {} } }))
beforeEach(() => vi.resetModules())

test('factoryPayment api exports', async () => {
  const m = await import('../api/factoryPayment')
  expect(typeof m.listPayments).toBe('function')
  expect(typeof m.createPayment).toBe('function')
  expect(typeof m.createRecord).toBe('function')
  expect(typeof m.generateByOrder).toBe('function')
  expect(typeof m.getStatement).toBe('function')
})
```

- [ ] **Step 2: 运行验证失败**

Run: `export PATH="/d/Pad Windows Data/nodejs/node-v22.23.2-win-arm64:$PATH" && cd frontend && npx vitest run src/tests/factoryPayment.test.ts --test-timeout=30000`
Expected: FAIL

- [ ] **Step 3: 实现 api/factoryPayment.ts**

```ts
import request from './request'
const base = '/factory-payment'

export const listPayments = (p?: any) =>
  request.get(`${base}/payments/`, { params: p })
export const getPayment = (id: number) =>
  request.get(`${base}/payments/${id}/`)
export const createPayment = (d: any) =>
  request.post(`${base}/payments/`, d)
export const updatePayment = (id: number, d: any) =>
  request.patch(`${base}/payments/${id}/`, d)
export const deletePayment = (id: number) =>
  request.delete(`${base}/payments/${id}/`)

export const listRecords = (p?: any) =>
  request.get(`${base}/records/`, { params: p })
export const createRecord = (d: any) =>
  request.post(`${base}/records/`, d)
export const deleteRecord = (id: number) =>
  request.delete(`${base}/records/${id}/`)

export const generateByOrder = (orderId: number) =>
  request.post(`${base}/payments/orders/${orderId}/generate/`)

export const getStatement = (p?: any) =>
  request.get(`${base}/payments/statement/`, { params: p })
```

- [ ] **Step 4: 路由 + 菜单**

`router/index.ts`：MainLayout 子路由加 `/factory-payment`（Placeholder，Task 5 替换）、`/factory-payment/statement`（Placeholder）；MobileLayout 子路由加 `/m/factory-payment`（Placeholder）、`/m/factory-payment/statement`（Placeholder）。
`MainLayout.vue`：菜单加"工厂结算"子菜单（结算单 /factory-payment、对账单 /factory-payment/statement）。
`MobileLayout.vue`：tabbar 加"工厂结算"项。

- [ ] **Step 5: 运行测试通过**

Run: `cd frontend && npx vitest run --test-timeout=30000`
Expected: PASS（无回归）

- [ ] **Step 6: Commit**

```bash
git add frontend/src
git commit -m "feat(frontend): 工厂结算 API 封装 + 路由 + 菜单

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 5: PC 工厂结算列表 + 详情 + OrderDetail 结算区块

**Files:**
- Create: `frontend/src/views/factory_payment/{FactoryPaymentList,FactoryPaymentDetail}.vue`
- Modify: `frontend/src/views/orders/OrderDetail.vue`（加结算状态区块 + 生成按钮）
- Modify: `frontend/src/router/index.ts`（替换 Placeholder）

- [ ] **Step 1: 实现 FactoryPaymentList.vue**

Element Plus：el-table（order_no/product_no/factory_name/amount_cny/paid_amount/status tag/操作）+ 筛选（factory/status）+ 分页 + 对账单入口链接。复用 request.ts 全局错误 toast。

- [ ] **Step 2: 实现 FactoryPaymentDetail.vue**

结算单详情：主表字段 + 付款记录列表 el-table + 登记付款表单（amount + payment_date + note）→ createRecord → 成功 reload。复用 request.ts。

- [ ] **Step 3: OrderDetail.vue 加结算状态区块**

第二模块 OrderDetail.vue 加"工厂结算"区块：每产品行显示 status tag（未生成/未结/部分结/已结）+ 应付/已付金额 + "生成结算单"按钮（admin/finance，调 generateByOrder）→ 成功 reload。

- [ ] **Step 4: 路由替换**

`/factory-payment`→FactoryPaymentList（懒加载）、`/factory-payment/:id`→FactoryPaymentDetail（懒加载）。

- [ ] **Step 5: 验证**

Run: `export PATH=...node...:$PATH && cd frontend && npx vitest run --test-timeout=30000 && npm run build`
Expected: vitest 全绿、build 成功。

- [ ] **Step 6: Commit**

```bash
git add frontend/src
git commit -m "feat(frontend): PC 工厂结算列表 + 详情 + OrderDetail 结算区块

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 6: PC 工厂对账单

**Files:**
- Create: `frontend/src/views/factory_payment/FactoryStatement.vue`
- Modify: `frontend/src/router/index.ts`（替换 Placeholder）

- [ ] **Step 1: 实现 FactoryStatement.vue**

Element Plus：工厂下拉筛选 + 日期范围 el-date-picker + 汇总卡片（应付总额/已付/未付）+ 结算单 el-table 列表。调 getStatement + listPayments。复用 request.ts。

- [ ] **Step 2: 路由替换**

`/factory-payment/statement`→FactoryStatement（懒加载）。

- [ ] **Step 3: 验证**

Run: `cd frontend && npx vitest run --test-timeout=30000 && npm run build`
Expected: vitest 全绿、build 成功。

- [ ] **Step 4: Commit**

```bash
git add frontend/src
git commit -m "feat(frontend): PC 工厂对账单

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 7: 移动端工厂结算列表 + 详情 + 对账单

**Files:**
- Create: `frontend/src/views/m/factory_payment/{MFactoryPaymentList,MFactoryPaymentDetail,MFactoryStatement}.vue`
- Modify: `frontend/src/router/index.ts`（替换 Placeholder）

- [ ] **Step 1: 实现 MFactoryPaymentList.vue**

Vant：van-list 结算单列表（order_no/factory_name/amount_cny/paid_amount/status tag）+ 工厂筛选 van-dropdown + 对账入口 + 详情入口。复用 request.ts。

- [ ] **Step 2: 实现 MFactoryPaymentDetail.vue**

Vant：van-cell-group 结算单详情 + 付款记录 van-cell + 登记付款 van-form（amount + payment_date + note）→ createRecord → 成功 reload。

- [ ] **Step 3: 实现 MFactoryStatement.vue**

Vant：工厂筛选 van-dropdown + 日期范围 + 汇总 van-grid（应付/已付/未付）+ van-list 结算单列表。

- [ ] **Step 4: 路由替换**

`/m/factory-payment`→MFactoryPaymentList、`/m/factory-payment/:id`→MFactoryPaymentDetail、`/m/factory-payment/statement`→MFactoryStatement（均懒加载）。

- [ ] **Step 5: 验证**

Run: `cd frontend && npx vitest run --test-timeout=30000 && npm run build`
Expected: vitest 全绿、build 成功。

- [ ] **Step 6: Commit**

```bash
git add frontend/src
git commit -m "feat(frontend): 移动端工厂结算（完整功能）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 8: 集成验证 + neat-freak

- [ ] **Step 1: 后端全量测试**

Run: `cd backend && python manage.py check && python -m pytest -v`
Expected: 全绿（含前三模块无回归 + 工厂结算模块全部）

- [ ] **Step 2: 前端全量测试 + build**

Run: `export PATH=...node...:$PATH && cd frontend && npx vitest run --test-timeout=30000 && npm run build`
Expected: 全绿、build 成功。

- [ ] **Step 3: 本地联调**

后端 runserver + 前端 dev，登录后：工厂结算列表/详情/付款登记 + 一键生成（订单详情点"生成结算单"）+ 工厂对账单 + 移动端完整功能。

- [ ] **Step 4: final whole-branch review (fable) + fix**

派 final code reviewer（fable）审整分支；处理 Critical/Important（ONE fix dispatch sonnet + ONE re-review fable）。

- [ ] **Step 5: neat-freak 整理**

调 neat-freak skill：更新 README/设计文档，清理残留。

- [ ] **Step 6: 汇报中间文件 + 合并**

汇报，合并 feature/factory-payment 到 main + 清理。