# 基础信息模块实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付项目骨架 + 基础信息 6 表单 CRUD + 产品库/工厂库 Excel 批量导入，本地可运行。

**Architecture:** Monorepo（backend Django+DRF / frontend Vue3+Vite）。后端按业务域分 app，JWT 认证，统一响应与异常。前端 PC 用 Element Plus、移动端用 Vant，按 `/m/` 路由分流。TDD：先测试后实现。

**Tech Stack:** Python 3.13 + Django 5.1+ + DRF + simplejwt + MySQL 8 + openpyxl + pytest；Vue3 + Vite + TS + Element Plus + Vant + Pinia + Vue Router + axios + Vitest。

## Global Constraints

- 所有文件创建在工作目录 `D:\Pad Windows Data\Claude项目\xb项目`。
- Python 解释器：`D:\Pad Windows Data\Python313-arm64\python.exe`；用 `python -m pip` 装包。
- Django 必须 5.1+（支持 Python 3.13）。
- 后端测试用 pytest，前端用 vitest；每个任务结束前测试全绿。
- 中文路径：git 已设 `core.quotepath false`。
- 数据库：MySQL 8（开发环境库名 `xb_dev`，用户 `xb` / 密码开发期用 `xb123456`，仅本地）。
- 提交信息末尾加 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。
- 前端运行在 5173，后端运行在 8000；Vite 代理 `/api` 到 8000。

## File Structure

**后端：**
- `backend/manage.py`
- `backend/xb_project/settings/{base,dev,prod}.py`、`urls.py`、`wsgi.py`
- `backend/apps/basic_info/{models,serializers,views,urls,admin}.py`
- `backend/apps/basic_info/tests/{test_category,test_product,test_factory,test_logistics,test_customer,test_import}.py`
- `backend/apps/accounts/{models,serializers,views,urls}.py`、`tests/test_auth.py`
- `backend/common/{response,exceptions,permissions,pagination}.py`
- `backend/requirements/{dev,prod}.txt`、`backend/pytest.ini`、`backend/.env.example`

**前端：**
- `frontend/{package.json,vite.config.ts,tsconfig.json,index.html}`
- `frontend/src/{main.ts,App.vue}`
- `frontend/src/router/index.ts`
- `frontend/src/stores/user.ts`
- `frontend/src/api/{request,auth,basicInfo}.ts`
- `frontend/src/layouts/{MainLayout,MobileLayout}.vue`
- `frontend/src/views/Login.vue`
- `frontend/src/views/basic_info/{Category,Product,Factory,Logistics,Customer}Manage.vue`
- `frontend/src/views/system/UserManage.vue`
- `frontend/src/views/m/{MobileHome,MProductList,MFactoryList,MCustomerList}.vue`
- `frontend/src/tests/{user.test.ts,request.test.ts}`

---

## Task 1: 环境准备与后端骨架

**Files:**
- Create: `backend/manage.py`, `backend/xb_project/*`, `backend/requirements/*`, `backend/pytest.ini`, `backend/.env.example`

**Interfaces:**
- Produces: 可运行的 Django 空项目，`python manage.py runserver` 启动，连接 MySQL `xb_dev`。

- [ ] **Step 1: 安装 MySQL 8 并建库**

下载 MySQL 8 安装包到 `D:\Pad Windows Data\mysql8`，引导安装（仅本地开发）。建库建用户：
```sql
CREATE DATABASE xb_dev CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'xb'@'localhost' IDENTIFIED BY 'xb123456';
GRANT ALL ON xb_dev.* TO 'xb'@'localhost';
```

- [ ] **Step 2: 安装后端依赖**

`backend/requirements/dev.txt`：
```
Django>=5.1,<5.2
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
mysqlclient>=2.2
openpyxl>=3.1
python-dotenv>=1.0
pytest>=8.0
pytest-django>=4.8
factory_boy>=3.3
```

Run: `python -m pip install -r backend/requirements/dev.txt`

- [ ] **Step 3: 生成 Django 项目骨架**

```bash
cd backend
python -m django startproject xb_project .
```

- [ ] **Step 4: 拆分 settings 为 base/dev/prod**

`backend/xb_project/settings/base.py`（含 INSTALLED_APPS 加 rest_framework、apps.basic_info、apps.accounts；AUTH 预留；DATABASES 留空由 dev/prod 覆盖；REST_FRAMEWORK 默认分页器指向 `common.pagination.StandardResultsSetPagination`、默认 AUTH 用 JWT、EXCEPTION_HANDLER 指向 `common.exceptions.custom_exception_handler`）。

`backend/xb_project/settings/dev.py`：`from .base import *`，`DEBUG=True`，`DATABASES` 指 MySQL `xb_dev`，`ALLOWED_HOSTS=['*']`，`CORS`/Vite 代理注意。

`backend/xb_project/settings/prod.py`：`from .base import *`，`DEBUG=False`，从环境变量读 DB。

`backend/manage.py`：`os.environ.setdefault('DJANGO_SETTINGS_MODULE','xb_project.settings.dev')`。

- [ ] **Step 5: 配置 pytest 与 .env**

`backend/pytest.ini`：
```ini
[pytest]
DJANGO_SETTINGS_MODULE = xb_project.settings.dev
python_files = tests.py test_*.py *_tests.py
```

`backend/.env.example`：列出 `DB_NAME/DB_USER/DB_PASSWORD/SECRET_KEY` 键名（值留空）。

- [ ] **Step 6: 验证启动**

Run: `cd backend && python manage.py migrate && python manage.py runserver`
Expected: 无报错，访问 `http://127.0.0.1:8000/` 返回 Django 默认页（或 404）。

- [ ] **Step 7: Commit**

```bash
git add backend/
git commit -m "feat(backend): Django 项目骨架与 MySQL 配置

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 2: 通用库（统一响应、异常处理、分页、权限基类）

**Files:**
- Create: `backend/common/response.py`, `common/exceptions.py`, `common/pagination.py`, `common/permissions.py`
- Test: `backend/common/tests/test_response.py`

**Interfaces:**
- Produces: `success_response(data, message='success')`、`error_response(code, message, status)`、`StandardResultsSetPagination`、`custom_exception_handler(exc, context)`、`RolePermission`（按 group 名校验）。

- [ ] **Step 1: 写失败测试**

`backend/common/tests/test_response.py`：
```python
from common.response import success_response, error_response
from rest_framework.response import Response

def test_success_response():
    r = success_response({'a': 1})
    assert isinstance(r, Response)
    assert r.data == {'code': 0, 'message': 'success', 'data': {'a': 1}}

def test_error_response():
    r = error_response(1001, 'invalid', status=400)
    assert r.status_code == 400
    assert r.data == {'code': 1001, 'message': 'invalid', 'data': None}
```

- [ ] **Step 2: 运行验证失败**

Run: `cd backend && python -m pytest common/tests/test_response.py -v`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 实现 common/response.py**

```python
from rest_framework.response import Response

def success_response(data=None, message='success'):
    return Response({'code': 0, 'message': message, 'data': data})

def error_response(code, message, status=400, data=None):
    return Response({'code': code, 'message': message, 'data': data}, status=status)
```

- [ ] **Step 4: 实现 common/pagination.py**

```python
from rest_framework.pagination import PageNumberPagination

class StandardResultsSetPagination(PageNumberPagination):
    page_size = 20
    page_size_query_param = 'page_size'
    max_page_size = 200
```

- [ ] **Step 5: 实现 common/exceptions.py**

```python
from rest_framework.views import exception_handler
from rest_framework.response import Response

ERROR_CODES = {
    400: 1001, 401: 1002, 403: 1003, 404: 1004, 500: 5000,
}

def custom_exception_handler(exc, context):
    response = exception_handler(exc, context)
    if response is not None:
        code = ERROR_CODES.get(response.status_code, 5000)
        if isinstance(response.data, dict) and 'detail' in response.data:
            message = response.data['detail']
        elif isinstance(response.data, dict):
            message = response.data
        else:
            message = str(response.data)
        response.data = {'code': code, 'message': message, 'data': None}
    return response
```

- [ ] **Step 6: 实现 common/permissions.py**

```python
from rest_framework.permissions import BasePermission

class RolePermission(BasePermission):
    allowed_roles = []  # 子类填 group 名

    def has_permission(self, request, view):
        if not request.user or not request.user.is_authenticated:
            return False
        groups = set(request.user.groups.values_list('name', flat=True))
        if 'admin' in groups:
            return True
        return bool(set(self.allowed_roles) & groups)

class AdminWriteOthersReadOnly(RolePermission):
    def has_permission(self, request, view):
        if not request.user or not request.user.is_authenticated:
            return False
        groups = set(request.user.groups.values_list('name', flat=True))
        if 'admin' in groups:
            return True
        if request.method in ('GET', 'HEAD', 'OPTIONS'):
            return True
        return False
```

- [ ] **Step 7: 运行测试通过**

Run: `cd backend && python -m pytest common/tests/test_response.py -v`
Expected: PASS。

- [ ] **Step 8: Commit**

```bash
git add backend/common/
git commit -m "feat(common): 统一响应、异常处理、分页、权限基类

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 3: 认证模块（JWT + 用户 + 4 角色 + login/me + 权限）

**Files:**
- Create: `backend/apps/accounts/{__init__,apps,models,serializers,views,urls}.py`, `apps/accounts/management/commands/create_groups.py`, `apps/accounts/tests/test_auth.py`
- Modify: `backend/xb_project/settings/base.py`（SIMPLE_JWT 配置）、`backend/xb_project/urls.py`（挂载 accounts 路由）

**Interfaces:**
- Produces: `POST /api/auth/login/`、`POST /api/auth/refresh/`、`POST /api/auth/logout/`、`GET /api/auth/me/`；管理命令 `python manage.py create_groups` 创建 admin/salesman/tracker/finance 4 个 Group。

- [ ] **Step 1: 写失败测试**

`backend/apps/accounts/tests/test_auth.py`：
```python
import pytest
from django.contrib.auth.models import User, Group
from rest_framework.test import APIClient

@pytest.fixture
def client():
    return APIClient()

@pytest.fixture
def admin_user(db):
    u = User.objects.create_user('admin1', password='pw123456')
    u.groups.add(Group.objects.get(name='admin'))
    return u

def test_login_returns_tokens(client, admin_user):
    r = client.post('/api/auth/login/', {'username': 'admin1', 'password': 'pw123456'}, format='json')
    assert r.status_code == 200
    assert 'access' in r.data['data'] and 'refresh' in r.data['data']

def test_me_returns_user_and_roles(client, admin_user):
    client.force_authenticate(admin_user)
    r = client.get('/api/auth/me/')
    assert r.status_code == 200
    assert r.data['data']['username'] == 'admin1'
    assert 'admin' in r.data['data']['roles']

def test_create_groups_command(db):
    from django.core.management import call_command
    call_command('create_groups')
    assert Group.objects.filter(name='admin').exists()
    assert Group.objects.filter(name='salesman').exists()
    assert Group.objects.filter(name='tracker').exists()
    assert Group.objects.filter(name='finance').exists()
```

- [ ] **Step 2: 运行验证失败**

Run: `cd backend && python -m pytest apps/accounts/tests/test_auth.py -v`
Expected: FAIL（路由不存在、命令不存在）。

- [ ] **Step 3: 实现 serializers.py**

```python
from django.contrib.auth.models import User
from rest_framework import serializers

class LoginSerializer(serializers.Serializer):
    username = serializers.CharField()
    password = serializers.CharField(write_only=True)

class UserSerializer(serializers.ModelSerializer):
    roles = serializers.SerializerMethodField()
    class Meta:
        model = User
        fields = ['id', 'username', 'email', 'roles']
    def get_roles(self, obj):
        return list(obj.groups.values_list('name', flat=True))
```

- [ ] **Step 4: 实现 views.py**

```python
from rest_framework.views import APIView
from rest_framework.permissions import AllowAny, IsAuthenticated
from rest_framework_simplejwt.tokens import RefreshToken
from django.contrib.auth import authenticate
from common.response import success_response, error_response
from .serializers import LoginSerializer, UserSerializer

class LoginView(APIView):
    permission_classes = [AllowAny]
    def post(self, request):
        s = LoginSerializer(data=request.data)
        s.is_valid(raise_exception=True)
        user = authenticate(username=s.validated_data['username'], password=s.validated_data['password'])
        if not user:
            return error_response(1002, '用户名或密码错误', status=401)
        refresh = RefreshToken.for_user(user)
        return success_response({
            'access': str(refresh.access_token),
            'refresh': str(refresh),
        })

class MeView(APIView):
    permission_classes = [IsAuthenticated]
    def get(self, request):
        return success_response(UserSerializer(request.user).data)

class LogoutView(APIView):
    permission_classes = [IsAuthenticated]
    def post(self, request):
        try:
            RefreshToken(request.data.get('refresh')).blacklist()
        except Exception:
            pass
        return success_response(None, '已登出')
```

- [ ] **Step 5: 实现 urls.py**

```python
from rest_framework_simplejwt.views import TokenRefreshView
from rest_framework.routers import DefaultRouter
from .views import LoginView, MeView, LogoutView
from django.urls import path

urlpatterns = [
    path('login/', LoginView.as_view()),
    path('refresh/', TokenRefreshView.as_view()),
    path('logout/', LogoutView.as_view()),
    path('me/', MeView.as_view()),
]
```

- [ ] **Step 6: 实现 create_groups 命令**

`backend/apps/accounts/management/commands/create_groups.py`：
```python
from django.core.management.base import BaseCommand
from django.contrib.auth.models import Group

class Command(BaseCommand):
    def handle(self, *args, **kwargs):
        for name in ['admin', 'salesman', 'tracker', 'finance']:
            Group.objects.get_or_create(name=name)
        self.stdout.write('groups created')
```

- [ ] **Step 7: 挂载路由 + settings 配置**

`backend/xb_project/urls.py`：
```python
from django.urls import path, include
urlpatterns = [
    path('api/auth/', include('apps.accounts.urls')),
]
```

`base.py` 加：
```python
from datetime import timedelta
SIMPLE_JWT = {'ACCESS_TOKEN_LIFETIME': timedelta(hours=8), 'REFRESH_TOKEN_LIFETIME': timedelta(days=7)}
INSTALLED_APPS += ['rest_framework_simplejwt.token_blacklist']
```

- [ ] **Step 8: 运行测试通过**

Run: `cd backend && python manage.py create_groups && python -m pytest apps/accounts/tests/test_auth.py -v`
Expected: PASS。

- [ ] **Step 9: Commit**

```bash
git add backend/apps/accounts backend/xb_project
git commit -m "feat(auth): JWT 登录、me、4 角色创建命令

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 4: 类目 Category 模型与 API（含 tree）

**Files:**
- Create: `backend/apps/basic_info/models.py`（Category 部分）、`serializers.py`、`views.py`、`urls.py`、`admin.py`
- Test: `backend/apps/basic_info/tests/test_category.py`
- Modify: `backend/xb_project/urls.py`（挂载 basic_info 路由）

**Interfaces:**
- Produces: `GET/POST /api/basic-info/categories/`、`GET/PUT/DELETE /api/basic-info/categories/{id}/`、`GET /api/basic-info/categories/tree/`。

- [ ] **Step 1: 写失败测试**

`backend/apps/basic_info/tests/test_category.py`：
```python
import pytest
from django.contrib.auth.models import User, Group
from rest_framework.test import APIClient

@pytest.fixture
def admin_client(db):
    u = User.objects.create_user('admin1', password='pw123456')
    u.groups.add(Group.objects.get(name='admin'))
    c = APIClient(); c.force_authenticate(u); return c

def test_create_category(admin_client):
    r = admin_client.post('/api/basic-info/categories/', {'name': '包装类'}, format='json')
    assert r.status_code == 201 and r.data['data']['name'] == '包装类'

def test_tree(admin_client):
    parent = admin_client.post('/api/basic-info/categories/', {'name': '包装类'}, format='json').data['data']
    admin_client.post('/api/basic-info/categories/', {'name': '手提袋', 'parent': parent['id']}, format='json')
    r = admin_client.get('/api/basic-info/categories/tree/')
    assert r.status_code == 200
    tree = r.data['data']
    assert any(c['name'] == '包装类' and any(ch['name'] == '手提袋' for ch in c['children']) for c in tree)

def test_non_admin_cannot_write(db):
    u = User.objects.create_user('sales1', password='pw123456')
    u.groups.add(Group.objects.get(name='salesman'))
    c = APIClient(); c.force_authenticate(u)
    r = c.post('/api/basic-info/categories/', {'name': 'X'}, format='json')
    assert r.status_code == 403
```

- [ ] **Step 2: 运行验证失败**

Run: `cd backend && python -m pytest apps/basic_info/tests/test_category.py -v`
Expected: FAIL。

- [ ] **Step 3: 实现 models.py（Category）**

```python
from django.db import models

class Category(models.Model):
    name = models.CharField(max_length=64)
    parent = models.ForeignKey('self', null=True, blank=True, on_delete=models.CASCADE, related_name='children')
    sort_order = models.IntegerField(default=0)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    class Meta:
        ordering = ['sort_order', 'id']

    def __str__(self):
        return self.name
```

Run: `python manage.py makemigrations basic_info && python manage.py migrate`

- [ ] **Step 4: 实现 serializers.py**

```python
from rest_framework import serializers
from .models import Category

class CategorySerializer(serializers.ModelSerializer):
    class Meta:
        model = Category
        fields = ['id', 'name', 'parent', 'sort_order', 'created_at', 'updated_at']

class CategoryTreeSerializer(serializers.ModelSerializer):
    children = serializers.SerializerMethodField()
    class Meta:
        model = Category
        fields = ['id', 'name', 'parent', 'sort_order', 'children']
    def get_children(self, obj):
        return CategoryTreeSerializer(obj.children.all(), many=True).data
```

- [ ] **Step 5: 实现 views.py**

```python
from rest_framework import viewsets
from rest_framework.decorators import action
from rest_framework.permissions import IsAuthenticated
from common.permissions import AdminWriteOthersReadOnly
from common.response import success_response
from .models import Category
from .serializers import CategorySerializer, CategoryTreeSerializer

class CategoryViewSet(viewsets.ModelViewSet):
    queryset = Category.objects.all()
    serializer_class = CategorySerializer
    permission_classes = [IsAuthenticated, AdminWriteOthersReadOnly]
    filterset_fields = ['parent']
    search_fields = ['name']

    @action(detail=False, methods=['get'])
    def tree(self, request):
        roots = Category.objects.filter(parent__isnull=True)
        return success_response(CategoryTreeSerializer(roots, many=True).data)
```

- [ ] **Step 6: 实现 urls.py 并挂载**

`backend/apps/basic_info/urls.py`：
```python
from rest_framework.routers import DefaultRouter
from .views import CategoryViewSet
router = DefaultRouter()
router.register('categories', CategoryViewSet, basename='category')
urlpatterns = router.urls
```

`xb_project/urls.py` 加 `path('api/basic-info/', include('apps.basic_info.urls'))`。

- [ ] **Step 7: 运行测试通过**

Run: `cd backend && python -m pytest apps/basic_info/tests/test_category.py -v`
Expected: PASS。

- [ ] **Step 8: Commit**

```bash
git add backend/apps/basic_info backend/xb_project/urls.py
git commit -m "feat(basic_info): 类目模型与 API（含 tree）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 5: 产品库 Product 模型与 API

**Files:**
- Modify: `backend/apps/basic_info/models.py`（加 Product）、`serializers.py`、`views.py`、`urls.py`
- Test: `backend/apps/basic_info/tests/test_product.py`

**Interfaces:**
- Produces: `GET/POST /api/basic-info/products/`、`GET/PUT/DELETE /api/basic-info/products/{id}/`；字段 `product_no` 唯一。

- [ ] **Step 1: 写失败测试**

`backend/apps/basic_info/tests/test_product.py`：
```python
import pytest
from django.contrib.auth.models import User, Group
from rest_framework.test import APIClient

@pytest.fixture
def admin_client(db):
    u = User.objects.create_user('admin1', password='pw123456')
    u.groups.add(Group.objects.get(name='admin'))
    c = APIClient(); c.force_authenticate(u); return c

def test_create_product(admin_client):
    r = admin_client.post('/api/basic-info/products/', {
        'product_no': 'P001', 'model': 'M1', 'name': '名片',
        'spec': '90x54mm', 'default_price': '0.10', 'default_cost_price': '0.50'
    }, format='json')
    assert r.status_code == 201 and r.data['data']['product_no'] == 'P001'

def test_duplicate_product_no_rejected(admin_client):
    payload = {'product_no': 'P002', 'model': 'M', 'name': 'X'}
    admin_client.post('/api/basic-info/products/', payload, format='json')
    r = admin_client.post('/api/basic-info/products/', payload, format='json')
    assert r.status_code == 400

def test_filter_by_category(admin_client, db):
    from apps.basic_info.models import Category
    cat = Category.objects.create(name='C1')
    admin_client.post('/api/basic-info/products/', {'product_no': 'P1', 'name': 'a', 'category': cat.id}, format='json')
    r = admin_client.get(f'/api/basic-info/products/?category={cat.id}')
    assert r.status_code == 200 and len(r.data['data']['results']) >= 1
```

- [ ] **Step 2: 运行验证失败**

Run: `cd backend && python -m pytest apps/basic_info/tests/test_product.py -v`
Expected: FAIL。

- [ ] **Step 3: 实现 models.py（加 Product）**

```python
class Product(models.Model):
    product_no = models.CharField(max_length=64, unique=True)
    model = models.CharField(max_length=64, blank=True)
    name = models.CharField(max_length=128)
    category = models.ForeignKey(Category, null=True, blank=True, on_delete=models.SET_NULL, related_name='products')
    spec = models.TextField(blank=True)
    default_price = models.DecimalField(max_digits=12, decimal_places=2, default=0)      # USD
    default_cost_price = models.DecimalField(max_digits=12, decimal_places=2, default=0)  # CNY
    remark = models.TextField(blank=True)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    def __str__(self):
        return f'{self.product_no} {self.name}'
```

Run: `python manage.py makemigrations basic_info && python manage.py migrate`

- [ ] **Step 4: 实现 serializers.py（加 ProductSerializer）**

```python
from .models import Product

class ProductSerializer(serializers.ModelSerializer):
    class Meta:
        model = Product
        fields = ['id', 'product_no', 'model', 'name', 'category', 'spec',
                  'default_price', 'default_cost_price', 'remark', 'created_at', 'updated_at']
```

- [ ] **Step 5: 实现 views.py（加 ProductViewSet）**

```python
from .models import Product
from .serializers import ProductSerializer

class ProductViewSet(viewsets.ModelViewSet):
    queryset = Product.objects.all()
    serializer_class = ProductSerializer
    permission_classes = [IsAuthenticated, AdminWriteOthersReadOnly]
    filterset_fields = ['category']
    search_fields = ['product_no', 'model', 'name']
    ordering_fields = ['id', 'updated_at']
```

- [ ] **Step 6: 注册路由**

`urls.py` 加：`router.register('products', ProductViewSet, basename='product')`

- [ ] **Step 7: 运行测试通过**

Run: `cd backend && python -m pytest apps/basic_info/tests/test_product.py -v`
Expected: PASS。

- [ ] **Step 8: Commit**

```bash
git add backend/apps/basic_info
git commit -m "feat(basic_info): 产品库模型与 API

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 6: 工厂库 Factory 模型与 API

**Files:**
- Modify: `backend/apps/basic_info/models.py`、`serializers.py`、`views.py`、`urls.py`
- Test: `backend/apps/basic_info/tests/test_factory.py`

**Interfaces:**
- Produces: `GET/POST /api/basic-info/factories/` 等；`name` 唯一，`alias` 用于映射。

- [ ] **Step 1: 写失败测试**

`backend/apps/basic_info/tests/test_factory.py`：
```python
import pytest
from django.contrib.auth.models import User, Group
from rest_framework.test import APIClient

@pytest.fixture
def admin_client(db):
    u = User.objects.create_user('admin1', password='pw123456')
    u.groups.add(Group.objects.get(name='admin'))
    c = APIClient(); c.force_authenticate(u); return c

def test_create_factory(admin_client):
    r = admin_client.post('/api/basic-info/factories/', {
        'name': '华鑫', 'alias': '外部YT', 'contact': '张三', 'phone': '13800000000'
    }, format='json')
    assert r.status_code == 201 and r.data['data']['name'] == '华鑫'

def test_duplicate_name_rejected(admin_client):
    admin_client.post('/api/basic-info/factories/', {'name': '华鑫'}, format='json')
    r = admin_client.post('/api/basic-info/factories/', {'name': '华鑫'}, format='json')
    assert r.status_code == 400

def test_search_by_alias(admin_client):
    admin_client.post('/api/basic-info/factories/', {'name': '华鑫', 'alias': '外部YT'}, format='json')
    r = admin_client.get('/api/basic-info/factories/?search=外部YT')
    assert r.status_code == 200 and len(r.data['data']['results']) >= 1
```

- [ ] **Step 2: 运行验证失败**

Run: `cd backend && python -m pytest apps/basic_info/tests/test_factory.py -v`
Expected: FAIL。

- [ ] **Step 3: 实现 models.py（加 Factory）**

```python
class Factory(models.Model):
    name = models.CharField(max_length=128, unique=True)
    alias = models.CharField(max_length=128, blank=True)
    contact = models.CharField(max_length=64, blank=True)
    phone = models.CharField(max_length=32, blank=True)
    settle_currency = models.CharField(max_length=8, default='CNY')
    remark = models.TextField(blank=True)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    def __str__(self):
        return self.name
```

Run: `python manage.py makemigrations basic_info && python manage.py migrate`

- [ ] **Step 4: 实现 serializers.py（加 FactorySerializer）**

```python
from .models import Factory

class FactorySerializer(serializers.ModelSerializer):
    class Meta:
        model = Factory
        fields = ['id', 'name', 'alias', 'contact', 'phone', 'settle_currency', 'remark', 'created_at', 'updated_at']
```

- [ ] **Step 5: 实现 views.py（加 FactoryViewSet）**

```python
from .models import Factory
from .serializers import FactorySerializer

class FactoryViewSet(viewsets.ModelViewSet):
    queryset = Factory.objects.all()
    serializer_class = FactorySerializer
    permission_classes = [IsAuthenticated, AdminWriteOthersReadOnly]
    search_fields = ['name', 'alias', 'contact']
    ordering_fields = ['id', 'updated_at']
```

- [ ] **Step 6: 注册路由**

`urls.py` 加：`router.register('factories', FactoryViewSet, basename='factory')`

- [ ] **Step 7: 运行测试通过**

Run: `cd backend && python -m pytest apps/basic_info/tests/test_factory.py -v`
Expected: PASS。

- [ ] **Step 8: Commit**

```bash
git add backend/apps/basic_info
git commit -m "feat(basic_info): 工厂库模型与 API

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 7: 物流服务商 LogisticsProvider 模型与 API

**Files:**
- Modify: `backend/apps/basic_info/models.py`、`serializers.py`、`views.py`、`urls.py`
- Test: `backend/apps/basic_info/tests/test_logistics.py`

**Interfaces:**
- Produces: `GET/POST /api/basic-info/logistics/` 等；`type` 为 domestic/international。

- [ ] **Step 1: 写失败测试**

`backend/apps/basic_info/tests/test_logistics.py`：
```python
import pytest
from django.contrib.auth.models import User, Group
from rest_framework.test import APIClient

@pytest.fixture
def admin_client(db):
    u = User.objects.create_user('admin1', password='pw123456')
    u.groups.add(Group.objects.get(name='admin'))
    c = APIClient(); c.force_authenticate(u); return c

def test_create_domestic(admin_client):
    r = admin_client.post('/api/basic-info/logistics/', {
        'name': '圆通', 'type': 'domestic', 'contact': '李四'
    }, format='json')
    assert r.status_code == 201 and r.data['data']['type'] == 'domestic'

def test_invalid_type_rejected(admin_client):
    r = admin_client.post('/api/basic-info/logistics/', {'name': 'X', 'type': 'space'}, format='json')
    assert r.status_code == 400

def test_filter_by_type(admin_client):
    admin_client.post('/api/basic-info/logistics/', {'name': 'DHL', 'type': 'international'}, format='json')
    r = admin_client.get('/api/basic-info/logistics/?type=international')
    assert r.status_code == 200 and len(r.data['data']['results']) >= 1
```

- [ ] **Step 2: 运行验证失败**

Run: `cd backend && python -m pytest apps/basic_info/tests/test_logistics.py -v`
Expected: FAIL。

- [ ] **Step 3: 实现 models.py（加 LogisticsProvider）**

```python
class LogisticsProvider(models.Model):
    TYPE_CHOICES = [('domestic', '国内'), ('international', '国际')]
    name = models.CharField(max_length=128)
    type = models.CharField(max_length=16, choices=TYPE_CHOICES)
    contact = models.CharField(max_length=64, blank=True)
    phone = models.CharField(max_length=32, blank=True)
    remark = models.TextField(blank=True)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    def __str__(self):
        return f'{self.name}({self.type})'
```

Run: `python manage.py makemigrations basic_info && python manage.py migrate`

- [ ] **Step 4: 实现 serializers.py（加 LogisticsProviderSerializer）**

```python
from .models import LogisticsProvider

class LogisticsProviderSerializer(serializers.ModelSerializer):
    class Meta:
        model = LogisticsProvider
        fields = ['id', 'name', 'type', 'contact', 'phone', 'remark', 'created_at', 'updated_at']
```

- [ ] **Step 5: 实现 views.py（加 LogisticsProviderViewSet）**

```python
from .models import LogisticsProvider
from .serializers import LogisticsProviderSerializer

class LogisticsProviderViewSet(viewsets.ModelViewSet):
    queryset = LogisticsProvider.objects.all()
    serializer_class = LogisticsProviderSerializer
    permission_classes = [IsAuthenticated, AdminWriteOthersReadOnly]
    filterset_fields = ['type']
    search_fields = ['name', 'contact']
    ordering_fields = ['id', 'updated_at']
```

- [ ] **Step 6: 注册路由**

`urls.py` 加：`router.register('logistics', LogisticsProviderViewSet, basename='logistics')`

- [ ] **Step 7: 运行测试通过**

Run: `cd backend && python -m pytest apps/basic_info/tests/test_logistics.py -v`
Expected: PASS。

- [ ] **Step 8: Commit**

```bash
git add backend/apps/basic_info
git commit -m "feat(basic_info): 物流服务商模型与 API

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 8: 客户 Customer 模型与 API（含 salesman 筛选）

**Files:**
- Modify: `backend/apps/basic_info/models.py`、`serializers.py`、`views.py`、`urls.py`
- Test: `backend/apps/basic_info/tests/test_customer.py`

**Interfaces:**
- Produces: `GET/POST /api/basic-info/customers/` 等；`GET /api/basic-info/customers/?salesman=<id>` 按业务员筛选。

- [ ] **Step 1: 写失败测试**

`backend/apps/basic_info/tests/test_customer.py`：
```python
import pytest
from django.contrib.auth.models import User, Group
from rest_framework.test import APIClient

@pytest.fixture
def admin_client(db):
    u = User.objects.create_user('admin1', password='pw123456')
    u.groups.add(Group.objects.get(name='admin'))
    c = APIClient(); c.force_authenticate(u); return c

def test_create_customer_with_salesman(admin_client, db):
    sales = User.objects.create_user('sales1', password='pw123456')
    r = admin_client.post('/api/basic-info/customers/', {
        'name': '客户A', 'contact_person': '王五', 'salesman': sales.id
    }, format='json')
    assert r.status_code == 201 and r.data['data']['salesman'] == sales.id

def test_filter_by_salesman(admin_client, db):
    s1 = User.objects.create_user('sales1', password='pw123456')
    s2 = User.objects.create_user('sales2', password='pw123456')
    admin_client.post('/api/basic-info/customers/', {'name': 'A', 'salesman': s1.id}, format='json')
    admin_client.post('/api/basic-info/customers/', {'name': 'B', 'salesman': s2.id}, format='json')
    r = admin_client.get(f'/api/basic-info/customers/?salesman={s1.id}')
    assert r.status_code == 200
    assert all(c['salesman'] == s1.id for c in r.data['data']['results'])
```

- [ ] **Step 2: 运行验证失败**

Run: `cd backend && python -m pytest apps/basic_info/tests/test_customer.py -v`
Expected: FAIL。

- [ ] **Step 3: 实现 models.py（加 Customer）**

```python
from django.contrib.auth.models import User

class Customer(models.Model):
    name = models.CharField(max_length=128)
    contact_person = models.CharField(max_length=64, blank=True)
    phone = models.CharField(max_length=32, blank=True)
    email = models.CharField(max_length=128, blank=True)
    salesman = models.ForeignKey(User, null=True, blank=True, on_delete=models.SET_NULL, related_name='customers')
    remark = models.TextField(blank=True)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    def __str__(self):
        return self.name
```

Run: `python manage.py makemigrations basic_info && python manage.py migrate`

- [ ] **Step 4: 实现 serializers.py（加 CustomerSerializer）**

```python
from .models import Customer

class CustomerSerializer(serializers.ModelSerializer):
    salesman_name = serializers.CharField(source='salesman.username', read_only=True, default='')
    class Meta:
        model = Customer
        fields = ['id', 'name', 'contact_person', 'phone', 'email', 'salesman', 'salesman_name', 'remark', 'created_at', 'updated_at']
```

- [ ] **Step 5: 实现 views.py（加 CustomerViewSet）**

```python
from .models import Customer
from .serializers import CustomerSerializer

class CustomerViewSet(viewsets.ModelViewSet):
    queryset = Customer.objects.all()
    serializer_class = CustomerSerializer
    permission_classes = [IsAuthenticated, AdminWriteOthersReadOnly]
    filterset_fields = ['salesman']
    search_fields = ['name', 'contact_person', 'phone']
    ordering_fields = ['id', 'updated_at']
```

- [ ] **Step 6: 注册路由**

`urls.py` 加：`router.register('customers', CustomerViewSet, basename='customer')`

- [ ] **Step 7: 运行测试通过**

Run: `cd backend && python -m pytest apps/basic_info/tests/test_customer.py -v`
Expected: PASS。

- [ ] **Step 8: Commit**

```bash
git add backend/apps/basic_info
git commit -m "feat(basic_info): 客户模型与 API（含业务员筛选）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 9: 产品库 Excel 批量导入

**Files:**
- Create: `backend/apps/basic_info/importers.py`、`templates/basic_info/product_import.xlsx`（用代码生成模板）
- Modify: `backend/apps/basic_info/views.py`（加 import + import-template action）、`serializers.py`（加导入结果序列化）
- Test: `backend/apps/basic_info/tests/test_import.py`

**Interfaces:**
- Produces: `POST /api/basic-info/products/import/`（multipart `file`）、`GET /api/basic-info/products/import-template/`（下载 xlsx）。返回 `{success_count, fail_count, failures:[{row, reason}]}`。

- [ ] **Step 1: 写失败测试**

`backend/apps/basic_info/tests/test_import.py`：
```python
import pytest
from io import BytesIO
import openpyxl
from django.contrib.auth.models import User, Group
from rest_framework.test import APIClient

def make_product_xlsx(rows):
    wb = openpyxl.Workbook(); ws = wb.active
    ws.append(['product_no', 'model', 'name', 'spec', 'default_price', 'default_cost_price'])
    for r in rows:
        ws.append(r)
    buf = BytesIO(); wb.save(buf); buf.seek(0)
    return buf

@pytest.fixture
def admin_client(db):
    u = User.objects.create_user('admin1', password='pw123456')
    u.groups.add(Group.objects.get(name='admin'))
    c = APIClient(); c.force_authenticate(u); return c

def test_import_products_upsert(admin_client):
    buf = make_product_xlsx([
        ['P001', 'M1', '名片', '90x54', '0.10', '0.50'],
        ['P002', 'M2', '传单', 'A5', '0.20', '1.00'],
    ])
    r = admin_client.post('/api/basic-info/products/import/', {'file': buf}, format='multipart')
    assert r.status_code == 200
    assert r.data['data']['success_count'] == 2 and r.data['data']['fail_count'] == 0

def test_import_products_repeat_upserts(admin_client, db):
    from apps.basic_info.models import Product
    buf = make_product_xlsx([['P001', 'M1', '名片', '', '0.10', '0.50']])
    admin_client.post('/api/basic-info/products/import/', {'file': buf}, format='multipart')
    buf2 = make_product_xlsx([['P001', 'M1', '名片彩版', '', '0.12', '0.55']])
    r = admin_client.post('/api/basic-info/products/import/', {'file': buf2}, format='multipart')
    assert r.data['data']['success_count'] == 1
    assert Product.objects.get(product_no='P001').name == '名片彩版'

def test_import_collects_failures(admin_client):
    buf = make_product_xlsx([
        ['', 'M', '无编号', '', '0.1', '0.5'],          # 缺 product_no
        ['P001', 'M', '名片', '', '0.1', '0.5'],
    ])
    r = admin_client.post('/api/basic-info/products/import/', {'file': buf}, format='multipart')
    assert r.data['data']['fail_count'] == 1
    assert r.data['data']['failures'][0]['row'] == 2  # Excel 行号

def test_import_template_download(admin_client):
    r = admin_client.get('/api/basic-info/products/import-template/')
    assert r.status_code == 200
    assert r['Content-Type'].find('spreadsheet') >= 0 or r['Content-Type'].find('excel') >= 0
```

- [ ] **Step 2: 运行验证失败**

Run: `cd backend && python -m pytest apps/basic_info/tests/test_import.py -v`
Expected: FAIL。

- [ ] **Step 3: 实现 importers.py**

```python
import openpyxl
from .models import Product

PRODUCT_COLUMNS = ['product_no', 'model', 'name', 'spec', 'default_price', 'default_cost_price']

def import_products(file):
    wb = openpyxl.load_workbook(file, data_only=True)
    ws = wb.active
    headers = [c.value for c in ws[1]]
    col_index = {name: i for i, name in enumerate(headers)}
    success, failures = 0, []
    for row_idx, row in enumerate(ws.iter_rows(min_row=2, values_only=True), start=2):
        try:
            data = {name: row[col_index[name]] for name in PRODUCT_COLUMNS if name in col_index}
            product_no = (data.get('product_no') or '').strip()
            if not product_no:
                raise ValueError('product_no 不能为空')
            obj, created = Product.objects.update_or_create(
                product_no=product_no,
                defaults={
                    'model': data.get('model') or '',
                    'name': data.get('name') or '',
                    'spec': data.get('spec') or '',
                    'default_price': data.get('default_price') or 0,
                    'default_cost_price': data.get('default_cost_price') or 0,
                },
            )
            success += 1
        except Exception as e:
            failures.append({'row': row_idx, 'reason': str(e)})
    return {'success_count': success, 'fail_count': len(failures), 'failures': failures}

def build_product_template():
    wb = openpyxl.Workbook(); ws = wb.active
    ws.append(PRODUCT_COLUMNS)
    ws.append(['P001', 'M1', '示例名片', '90x54mm', '0.10', '0.50'])
    import io
    buf = io.BytesIO(); wb.save(buf); buf.seek(0)
    return buf
```

- [ ] **Step 4: 实现 views（加 import action）**

`views.py` 在 ProductViewSet 加：
```python
from rest_framework.decorators import action
from django.http import HttpResponse
from .importers import import_products, build_product_template

    @action(detail=False, methods=['post'], url_path='import')
    def import_data(self, request):
        f = request.FILES.get('file')
        if not f:
            return error_response(1001, '未上传文件', status=400)
        result = import_products(f)
        return success_response(result)

    @action(detail=False, methods=['get'], url_path='import-template')
    def import_template(self, request):
        buf = build_product_template()
        resp = HttpResponse(buf, content_type='application/vnd.openxmlformats-officedocument.spreadsheetml.sheet')
        resp['Content-Disposition'] = 'attachment; filename=product_import_template.xlsx'
        return resp
```

（`views.py` 顶部需 `from common.response import success_response, error_response`，与 Task 4 一致。）

- [ ] **Step 5: 运行测试通过**

Run: `cd backend && python -m pytest apps/basic_info/tests/test_import.py -v`
Expected: PASS。

- [ ] **Step 6: Commit**

```bash
git add backend/apps/basic_info
git commit -m "feat(basic_info): 产品库 Excel 批量导入与模板下载

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 10: 工厂库 Excel 批量导入

**Files:**
- Modify: `backend/apps/basic_info/importers.py`、`views.py`
- Modify: `backend/apps/basic_info/tests/test_import.py`（加工厂导入测试）

**Interfaces:**
- Produces: `POST /api/basic-info/factories/import/`、`GET /api/basic-info/factories/import-template/`。

- [ ] **Step 1: 写失败测试**

在 `test_import.py` 加：
```python
def make_factory_xlsx(rows):
    wb = openpyxl.Workbook(); ws = wb.active
    ws.append(['name', 'alias', 'contact', 'phone', 'settle_currency'])
    for r in rows:
        ws.append(r)
    buf = BytesIO(); wb.save(buf); buf.seek(0)
    return buf

def test_import_factories_upsert(admin_client, db):
    from apps.basic_info.models import Factory
    buf = make_factory_xlsx([
        ['华鑫', '外部YT', '张三', '13800000000', 'CNY'],
        ['宏远', '外部HY', '', '', 'CNY'],
    ])
    r = admin_client.post('/api/basic-info/factories/import/', {'file': buf}, format='multipart')
    assert r.data['data']['success_count'] == 2 and r.data['data']['fail_count'] == 0
    assert Factory.objects.count() == 2

def test_import_factories_repeat_upserts(admin_client, db):
    from apps.basic_info.models import Factory
    buf = make_factory_xlsx([['华鑫', '外部YT', '', '', 'CNY']])
    admin_client.post('/api/basic-info/factories/import/', {'file': buf}, format='multipart')
    buf2 = make_factory_xlsx([['华鑫', '外部YT2', '李四', '', 'CNY']])
    admin_client.post('/api/basic-info/factories/import/', {'file': buf2}, format='multipart')
    assert Factory.objects.get(name='华鑫').alias == '外部YT2'

def test_import_factories_missing_name_fails(admin_client):
    buf = make_factory_xlsx([['', '别名', '', '', 'CNY']])
    r = admin_client.post('/api/basic-info/factories/import/', {'file': buf}, format='multipart')
    assert r.data['data']['fail_count'] == 1
```

- [ ] **Step 2: 运行验证失败**

Run: `cd backend && python -m pytest apps/basic_info/tests/test_import.py::test_import_factories_upsert -v`
Expected: FAIL。

- [ ] **Step 3: 实现 importers.py（加工厂导入）**

```python
from .models import Factory

FACTORY_COLUMNS = ['name', 'alias', 'contact', 'phone', 'settle_currency']

def import_factories(file):
    wb = openpyxl.load_workbook(file, data_only=True)
    ws = wb.active
    headers = [c.value for c in ws[1]]
    col_index = {name: i for i, name in enumerate(headers)}
    success, failures = 0, []
    for row_idx, row in enumerate(ws.iter_rows(min_row=2, values_only=True), start=2):
        try:
            data = {name: row[col_index[name]] for name in FACTORY_COLUMNS if name in col_index}
            name = (data.get('name') or '').strip()
            if not name:
                raise ValueError('name 不能为空')
            Factory.objects.update_or_create(
                name=name,
                defaults={
                    'alias': data.get('alias') or '',
                    'contact': data.get('contact') or '',
                    'phone': data.get('phone') or '',
                    'settle_currency': data.get('settle_currency') or 'CNY',
                },
            )
            success += 1
        except Exception as e:
            failures.append({'row': row_idx, 'reason': str(e)})
    return {'success_count': success, 'fail_count': len(failures), 'failures': failures}

def build_factory_template():
    wb = openpyxl.Workbook(); ws = wb.active
    ws.append(FACTORY_COLUMNS)
    ws.append(['华鑫', '外部YT', '张三', '13800000000', 'CNY'])
    import io
    buf = io.BytesIO(); wb.save(buf); buf.seek(0)
    return buf
```

- [ ] **Step 4: 实现 views（FactoryViewSet 加 import action）**

`views.py` 在 FactoryViewSet 加：
```python
from .importers import import_factories, build_factory_template

    @action(detail=False, methods=['post'], url_path='import')
    def import_data(self, request):
        f = request.FILES.get('file')
        if not f:
            return error_response(1001, '未上传文件', status=400)
        return success_response(import_factories(f))

    @action(detail=False, methods=['get'], url_path='import-template')
    def import_template(self, request):
        buf = build_factory_template()
        resp = HttpResponse(buf, content_type='application/vnd.openxmlformats-officedocument.spreadsheetml.sheet')
        resp['Content-Disposition'] = 'attachment; filename=factory_import_template.xlsx'
        return resp
```

- [ ] **Step 5: 运行测试通过**

Run: `cd backend && python -m pytest apps/basic_info/tests/test_import.py -v`
Expected: PASS（产品+工厂导入全绿）。

- [ ] **Step 6: Commit**

```bash
git add backend/apps/basic_info
git commit -m "feat(basic_info): 工厂库 Excel 批量导入与模板下载

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 11: 用户管理 API

**Files:**
- Modify: `backend/apps/accounts/serializers.py`、`views.py`、`urls.py`
- Test: `backend/apps/accounts/tests/test_user_manage.py`

**Interfaces:**
- Produces: `GET/POST /api/auth/users/`、`GET/PUT/DELETE /api/auth/users/{id}/`；`POST` 创建用户并分配 group（`groups` 字段）。

- [ ] **Step 1: 写失败测试**

`backend/apps/accounts/tests/test_user_manage.py`：
```python
import pytest
from django.contrib.auth.models import User, Group
from rest_framework.test import APIClient

@pytest.fixture
def admin_client(db):
    u = User.objects.create_user('admin1', password='pw123456')
    u.groups.add(Group.objects.get(name='admin'))
    c = APIClient(); c.force_authenticate(u); return c

def test_admin_creates_salesman(admin_client, db):
    r = admin_client.post('/api/auth/users/', {
        'username': 'sales_new', 'password': 'pw123456', 'groups': ['salesman']
    }, format='json')
    assert r.status_code == 201
    assert Group.objects.get(name='salesman') in User.objects.get(username='sales_new').groups.all()

def test_non_admin_cannot_access(db):
    u = User.objects.create_user('sales1', password='pw123456')
    u.groups.add(Group.objects.get(name='salesman'))
    c = APIClient(); c.force_authenticate(u)
    r = c.get('/api/auth/users/')
    assert r.status_code == 403
```

- [ ] **Step 2: 运行验证失败**

Run: `cd backend && python -m pytest apps/accounts/tests/test_user_manage.py -v`
Expected: FAIL。

- [ ] **Step 3: 实现 serializers.py（加 UserManageSerializer）**

```python
from django.contrib.auth.models import User, Group

class UserManageSerializer(serializers.ModelSerializer):
    groups = serializers.SlugRelatedField(queryset=Group.objects.all(), slug_field='name', many=True, required=False)
    password = serializers.CharField(write_only=True, required=True)
    class Meta:
        model = User
        fields = ['id', 'username', 'email', 'password', 'groups']
    def create(self, validated):
        groups = validated.pop('groups', [])
        user = User.objects.create_user(**validated)
        user.groups.set(groups)
        return user
    def update(self, instance, validated):
        groups = validated.pop('groups', None)
        password = validated.pop('password', None)
        for k, v in validated.items():
            setattr(instance, k, v)
        if password:
            instance.set_password(password)
        if groups is not None:
            instance.groups.set(groups)
        instance.save()
        return instance
```

- [ ] **Step 4: 实现 views.py（加 UserViewSet）**

```python
from rest_framework import viewsets
from django.contrib.auth.models import User
from common.permissions import RolePermission
from .serializers import UserManageSerializer

class UserViewSet(viewsets.ModelViewSet):
    queryset = User.objects.all()
    serializer_class = UserManageSerializer
    permission_classes = [IsAuthenticated, RolePermission]
    allowed_roles = []  # 仅 admin（RolePermission 对 admin 直接放行）
```

注：`RolePermission` 对 admin 放行，其他角色被拒。需在 `UserViewSet` 设 `permission_classes=[IsAuthenticated, RolePermission]`，且 `RolePermission` 默认非 admin 全拒（因 `allowed_roles=[]`）。

- [ ] **Step 5: 挂载路由**

`accounts/urls.py` 加：
```python
from .views import UserViewSet
router = DefaultRouter()
router.register('users', UserViewSet, basename='user')
urlpatterns += router.urls
```

- [ ] **Step 6: 运行测试通过**

Run: `cd backend && python -m pytest apps/accounts/tests/test_user_manage.py -v`
Expected: PASS。

- [ ] **Step 7: Commit**

```bash
git add backend/apps/accounts
git commit -m "feat(auth): 用户管理 API（CRUD + 角色分配，仅 admin）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 12: 前端骨架（Vite+TS + 登录 + 布局 + 路由 + axios + Pinia）

**Files:**
- Create: `frontend/*`（package.json、vite.config.ts、tsconfig.json、index.html、src/main.ts、App.vue、router/index.ts、stores/user.ts、api/request.ts、api/auth.ts、layouts/MainLayout.vue、layouts/MobileLayout.vue、views/Login.vue）
- Test: `frontend/src/tests/user.test.ts`

**Interfaces:**
- Produces: 前端可 `npm run dev` 启动；登录后存 token 到 Pinia + localStorage；axios 拦截器注入 token、按 code 处理 401；路由守卫拦截未登录。

- [ ] **Step 1: 安装 Node.js LTS**

下载 Node.js 22.x 安装包到 `D:\Pad Windows Data\nodejs` 并安装，验证 `node -v` / `npm -v`。

- [ ] **Step 2: 创建 Vite 项目并装依赖**

```bash
cd frontend
npm create vite@latest . -- --template vue-ts
npm install
npm install vue-router pinia axios element-plus vant
npm install -D @types/node unplugin-vue-components unplugin-auto-import vitest @vue/test-utils jsdom @vant/auto-import-resolver
```

- [ ] **Step 3: 配置 vite.config.ts（代理 + 按需引入）**

```ts
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import AutoImport from 'unplugin-auto-import/vite'
import Components from 'unplugin-vue-components/vite'
import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'
import { VantResolver } from '@vant/auto-import-resolver'

export default defineConfig({
  plugins: [vue(), AutoImport({ resolvers: [ElementPlusResolver()] }), Components({ resolvers: [ElementPlusResolver(), VantResolver()] })],
  server: { port: 5173, proxy: { '/api': { target: 'http://127.0.0.1:8000', changeOrigin: true } } },
})
```

- [ ] **Step 4: 写失败测试（user store）**

`frontend/src/tests/user.test.ts`：
```ts
import { setActivePinia, createPinia } from 'pinia'
import { useUserStore } from '../stores/user'

beforeEach(() => { setActivePinia(createPinia()); localStorage.clear() })

test('setToken stores token', () => {
  const s = useUserStore()
  s.setToken('abc')
  expect(s.token).toBe('abc')
  expect(localStorage.getItem('token')).toBe('abc')
})

test('logout clears token and roles', () => {
  const s = useUserStore()
  s.setToken('abc'); s.roles = ['admin']
  s.logout()
  expect(s.token).toBe(''); expect(s.roles).toEqual([])
})
```

- [ ] **Step 5: 运行验证失败**

Run: `cd frontend && npx vitest run src/tests/user.test.ts`
Expected: FAIL（store 未实现）。

- [ ] **Step 6: 实现 stores/user.ts**

```ts
import { defineStore } from 'pinia'

export const useUserStore = defineStore('user', {
  state: () => ({ token: localStorage.getItem('token') || '', roles: [] as string[], username: '' }),
  actions: {
    setToken(t: string) { this.token = t; localStorage.setItem('token', t) },
    setProfile(p: { username: string; roles: string[] }) { this.username = p.username; this.roles = p.roles },
    logout() { this.token = ''; this.roles = []; this.username = ''; localStorage.removeItem('token') },
  },
})
```

- [ ] **Step 7: 实现 api/request.ts**

```ts
import axios from 'axios'
import { useUserStore } from '../stores/user'

const request = axios.create({ baseURL: '/api', timeout: 15000 })

request.interceptors.request.use(cfg => {
  const u = useUserStore()
  if (u.token) cfg.headers.Authorization = `Bearer ${u.token}`
  return cfg
})

request.interceptors.response.use(
  res => res.data,
  err => {
    if (err.response?.status === 401) { useUserStore().logout(); location.href = '/login' }
    return Promise.reject(err)
  }
)
export default request
```

- [ ] **Step 8: 实现 api/auth.ts + router + Login + 布局**

`api/auth.ts`：
```ts
import request from './request'
export const login = (d: { username: string; password: string }) => request.post('/auth/login/', d)
export const me = () => request.get('/auth/me/')
```

`router/index.ts`：定义 `Login`、`MainLayout`（含子路由 basic_info/* 与 system/users）、`MobileLayout`（含子路由 m/*），路由守卫：无 token 重定向 `/login`，移动路由前缀 `/m`。

`views/Login.vue`：Element Plus 表单，调 `login` → `setToken` → 跳转 `/`。

`layouts/MainLayout.vue`：Element Plus container（aside 菜单 + header + main + router-view）。

`layouts/MobileLayout.vue`：Vant tabbar + router-view。

- [ ] **Step 9: 运行测试通过**

Run: `cd frontend && npx vitest run src/tests/user.test.ts`
Expected: PASS。

- [ ] **Step 10: 验证启动**

Run: `cd frontend && npm run dev`
Expected: 访问 `http://localhost:5173` 显示登录页（后端需同时运行）。

- [ ] **Step 11: Commit**

```bash
git add frontend/
git commit -m "feat(frontend): Vite+TS 骨架、登录、布局、路由、axios、Pinia

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 13: 前端 PC 基础信息页面（6 管理页）

**Files:**
- Create: `frontend/src/api/basicInfo.ts`、`views/basic_info/{Category,Product,Factory,Logistics,Customer}Manage.vue`
- Test: `frontend/src/tests/basicInfo.test.ts`

**Interfaces:**
- Consumes: 后端 `/api/basic-info/*`。
- Produces: 5 个管理页，挂在 MainLayout 子路由；产品库/工厂库含批量导入按钮与模板下载。

- [ ] **Step 1: 写失败测试（API 封装）**

`frontend/src/tests/basicInfo.test.ts`：
```ts
import { setActivePinia, createPinia } from 'pinia'
beforeEach(() => setActivePinia(createPinia()))
// 仅验证导出函数存在与签名（不调网络）
test('basicInfo api exports', async () => {
  const m = await import('../api/basicInfo')
  expect(typeof m.listCategories).toBe('function')
  expect(typeof m.importProducts).toBe('function')
  expect(typeof m.downloadProductTemplate).toBe('function')
})
```

- [ ] **Step 2: 运行验证失败**

Run: `cd frontend && npx vitest run src/tests/basicInfo.test.ts`
Expected: FAIL。

- [ ] **Step 3: 实现 api/basicInfo.ts**

```ts
import request from './request'
const base = '/basic-info'
export const listCategories = (p?: any) => request.get(`${base}/categories/`, { params: p })
export const createCategory = (d: any) => request.post(`${base}/categories/`, d)
export const updateCategory = (id: number, d: any) => request.patch(`${base}/categories/${id}/`, d)
export const deleteCategory = (id: number) => request.delete(`${base}/categories/${id}/`)
export const categoryTree = () => request.get(`${base}/categories/tree/`)
export const listProducts = (p?: any) => request.get(`${base}/products/`, { params: p })
export const createProduct = (d: any) => request.post(`${base}/products/`, d)
export const updateProduct = (id: number, d: any) => request.patch(`${base}/products/${id}/`, d)
export const deleteProduct = (id: number) => request.delete(`${base}/products/${id}/`)
export const importProducts = (file: File) => { const fd = new FormData(); fd.append('file', file); return request.post(`${base}/products/import/`, fd, { headers: { 'Content-Type': 'multipart/form-data' } }) }
export const downloadProductTemplate = () => request.get(`${base}/products/import-template/`, { responseType: 'blob' })
// Factory / Logistics / Customer 同模式（list/create/update/delete + factory import/template）
export const listFactories = (p?: any) => request.get(`${base}/factories/`, { params: p })
export const createFactory = (d: any) => request.post(`${base}/factories/`, d)
export const updateFactory = (id: number, d: any) => request.patch(`${base}/factories/${id}/`, d)
export const deleteFactory = (id: number) => request.delete(`${base}/factories/${id}/`)
export const importFactories = (file: File) => { const fd = new FormData(); fd.append('file', file); return request.post(`${base}/factories/import/`, fd, { headers: { 'Content-Type': 'multipart/form-data' } }) }
export const listLogistics = (p?: any) => request.get(`${base}/logistics/`, { params: p })
export const createLogistics = (d: any) => request.post(`${base}/logistics/`, d)
export const updateLogistics = (id: number, d: any) => request.patch(`${base}/logistics/${id}/`, d)
export const deleteLogistics = (id: number) => request.delete(`${base}/logistics/${id}/`)
export const listCustomers = (p?: any) => request.get(`${base}/customers/`, { params: p })
export const createCustomer = (d: any) => request.post(`${base}/customers/`, d)
export const updateCustomer = (id: number, d: any) => request.patch(`${base}/customers/${id}/`, d)
export const deleteCustomer = (id: number) => request.delete(`${base}/customers/${id}/`)
```

- [ ] **Step 4: 实现 ProductManage.vue**

Element Plus 表格 + 搜索 + 类目筛选 + 新增/编辑抽屉 + 批量导入（el-upload 调 `importProducts`，显示结果 `success_count/fail_count`）+ 模板下载按钮（`downloadProductTemplate` 转 blob 下载）。

- [ ] **Step 5: 实现 FactoryManage.vue**

同 Product，字段为 name/alias/contact/phone/settle_currency，含批量导入。

- [ ] **Step 6: 实现 CategoryManage.vue**

el-tree 展示 `categoryTree()` 结果，支持新增/编辑/删除节点。

- [ ] **Step 7: 实现 LogisticsManage.vue、CustomerManage.vue**

Logistics：表格 + type 筛选。Customer：表格 + salesman 筛选 + 新增/编辑含归属业务员下拉。

- [ ] **Step 8: 注册路由**

`router/index.ts` MainLayout 子路由加：`/basic-info/category`、`/basic-info/product`、`/basic-info/factory`、`/basic-info/logistics`、`/basic-info/customer`。

- [ ] **Step 9: 运行测试通过**

Run: `cd frontend && npx vitest run src/tests/basicInfo.test.ts`
Expected: PASS。

- [ ] **Step 10: Commit**

```bash
git add frontend/src
git commit -m "feat(frontend): PC 基础信息 5 个管理页与 API 封装

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 14: 前端用户管理页

**Files:**
- Create: `frontend/src/api/auth.ts`（加 user 管理 API）、`frontend/src/views/system/UserManage.vue`
- Modify: `frontend/src/router/index.ts`
- Test: `frontend/src/tests/userManage.test.ts`

**Interfaces:**
- Produces: `/system/users` 页，账号 CRUD + 角色分配（admin 专属）。

- [ ] **Step 1: 写失败测试**

`frontend/src/tests/userManage.test.ts`：
```ts
test('user admin api exports', async () => {
  const m = await import('../api/auth')
  expect(typeof m.listUsers).toBe('function')
  expect(typeof m.createUser).toBe('function')
  expect(typeof m.deleteUser).toBe('function')
})
```

- [ ] **Step 2: 运行验证失败**

Run: `cd frontend && npx vitest run src/tests/userManage.test.ts`
Expected: FAIL。

- [ ] **Step 3: 实现 api（auth.ts 追加）**

```ts
export const listUsers = (p?: any) => request.get('/auth/users/', { params: p })
export const createUser = (d: any) => request.post('/auth/users/', d)
export const updateUser = (id: number, d: any) => request.patch(`/auth/users/${id}/`, d)
export const deleteUser = (id: number) => request.delete(`/auth/users/${id}/`)
```

- [ ] **Step 4: 实现 UserManage.vue**

表格 + 新增/编辑（用户名/密码/角色多选）+ 删除。

- [ ] **Step 5: 注册路由 + 运行测试通过**

路由加 `/system/users`。Run: `npx vitest run src/tests/userManage.test.ts` → PASS。

- [ ] **Step 6: Commit**

```bash
git add frontend/src
git commit -m "feat(frontend): 用户管理页

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 15: 前端移动端（登录复用 + 首页 + 基础信息只读）

**Files:**
- Create: `frontend/src/views/m/{MobileHome,MProductList,MFactoryList,MCustomerList}.vue`
- Modify: `frontend/src/router/index.ts`（/m 子路由）、`layouts/MobileLayout.vue`

**Interfaces:**
- Produces: `/m` 首页 + `/m/products`、`/m/factories`、`/m/customers` 只读列表（Vant）。

- [ ] **Step 1: 实现 MobileLayout.vue**

Vant van-tabbar（首页/产品/工厂/客户/我的）+ router-view。

- [ ] **Step 2: 实现 4 个只读页面**

`MobileHome`：概览卡（入口）。`MProductList`/`MFactoryList`/`MCustomerList`：van-list 拉取只读列表（调 `listProducts`/`listFactories`/`listCustomers`），van-cell 展示。

- [ ] **Step 3: 注册 /m 路由**

`router/index.ts` 加 `/m`、`/m/products`、`/m/factories`、`/m/customers`，用 MobileLayout。

- [ ] **Step 4: 验证**

Run: `cd frontend && npm run dev`，浏览器用移动模拟访问 `http://localhost:5173/m`（登录后）显示首页与列表。

- [ ] **Step 5: Commit**

```bash
git add frontend/src
git commit -m "feat(frontend): 移动端骨架与基础信息只读浏览

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 16: 集成验证与 neat-freak 整理

**Files:**
- Run: 全量后端测试 + 全量前端测试 + 本地联调
- 调用 neat-freak skill 整理

- [ ] **Step 1: 后端全量测试**

Run: `cd backend && python -m pytest -v`
Expected: 全绿。

- [ ] **Step 2: 前端全量测试**

Run: `cd frontend && npx vitest run`
Expected: 全绿。

- [ ] **Step 3: 本地联调**

后端 `runserver` + 前端 `npm run dev`，浏览器登录 → 类目/产品/工厂/物流/客户增删改查 → 产品库批量导入 → 用户管理 → 移动端 `/m` 只读浏览。

- [ ] **Step 4: 调用 neat-freak 整理**

按 CLAUDE.md 规则，模块完成后调用 neat-freak skill 整理文档/规则/记忆与代码的一致性。

- [ ] **Step 5: 汇报中间文件并提交**

检查产生的中间文件路径，向用户汇报；提交收尾 commit。

---

## Self-Review 结果

- **Spec coverage**：设计文档 6 表 → Task 4-8；批量导入 → Task 9-10；认证 → Task 3；用户管理 → Task 11；前端 PC → Task 12-13；用户管理页 → Task 14；移动端 → Task 15；DoD → Task 16。全覆盖。
- **Placeholder scan**：无 TBD/TODO；每个代码步骤含实际代码。
- **Type consistency**：`success_response`/`error_response`、`RolePermission`/`AdminWriteOthersReadOnly`、`import_products`/`import_factories`、store `useUserStore` 跨任务命名一致。