# 物流管理模块实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付物流账单表（两段物流）+ 一订单多次发货 + 费用归属 + PC + 移动端完整功能。

**Architecture:** 新建 `apps/logistics`（Logistics FK→Order 多行 + LogisticsPermission）；前端 `views/logistics`（PC）+ `views/m/logistics`（移动端完整）+ OrderDetail 加物流区块。沿用前四模块基础设施。

**Tech Stack:** Python 3.13 + Django 5.1 + DRF + SQLite + pytest；Vue3 + Vite + TS + Element Plus + Vant + Pinia + axios + Vitest。implementer 用 sonnet，reviewer 用 fable。

## Global Constraints

- 工作目录 `D:\Pad Windows Data\Claude项目\xb项目`。分支策略：从 `dev` 拉出 `feature/logistics`，完成后合并回 `dev`。
- Python `D:\Pad Windows Data\Python313-arm64\python.exe`，`python -m pip`，装依赖经 `backend/requirements/dev.txt`。
- Django 5.1+，pytest。Node `D:\Pad Windows Data\nodejs\node-v22.23.2-win-arm64\`（export PATH）。npm `--registry=https://registry.npmmirror.com`。
- 提交尾行 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。
- 沿用前四模块：BaseModelViewSet、统一响应、django-filter、权限类。
- 测试 conftest autouse create_groups 模式；前端测试 @vitest-environment node + mock element-plus；vitest ARM --test-timeout=30000。
- 后端 8000、前端 5173、Vite 代理 /api。

## File Structure

**后端：**
- `backend/apps/logistics/{__init__,apps,models,serializers,views,permissions,urls,admin}.py`
- `backend/apps/logistics/migrations/`
- `backend/apps/logistics/tests/{conftest,test_models,test_api,test_permission}.py`
- `backend/xb_project/settings/base.py`（INSTALLED_APPS）
- `backend/xb_project/urls.py`

**前端：**
- `frontend/src/api/logistics.ts`
- `frontend/src/views/logistics/{LogisticsList,LogisticsForm}.vue`
- `frontend/src/views/m/logistics/{MLogisticsList,MLogisticsForm}.vue`
- `frontend/src/views/orders/OrderDetail.vue`（加物流区块）
- `frontend/src/router/index.ts`、`frontend/src/layouts/{MainLayout,MobileLayout}.vue`
- `frontend/src/tests/logistics.test.ts`

---

### Task 1: Logistics 模型 + migration

- Create: `backend/apps/logistics/{__init__,apps,models}.py`
- Modify: `backend/xb_project/settings/base.py`
- Test: `backend/apps/logistics/tests/{conftest,test_models}.py`

- [ ] Step 1: startapp logistics apps/logistics，INSTALLED_APPS 加 'apps.logistics'，apps.py name 改 'apps.logistics'
- [ ] Step 2: 写失败测试（conftest autouse create_groups + test_models seq 自动递增/payer 枚举/币种）
- [ ] Step 3: 验证失败
- [ ] Step 4: 实现 models.py（Logistics 字段按设计文档，save 时 seq 自动递增）
- [ ] Step 5: makemigrations logistics && migrate
- [ ] Step 6: 运行测试通过（全量无回归）
- [ ] Step 7: Commit

---

### Task 2: Serializer

- Create: `backend/apps/logistics/serializers.py`
- Test: `backend/apps/logistics/tests/test_serializer.py`

- [ ] Step 1: 写失败测试（嵌套 order_no/factory_name/carrier_name/intl_name 只读字段）
- [ ] Step 2: 验证失败
- [ ] Step 3: 实现 serializers.py
- [ ] Step 4: 运行测试通过
- [ ] Step 5: Commit

---

### Task 3: ViewSet + Permission + urls

- Create: `backend/apps/logistics/{permissions,views,urls}.py`
- Modify: `backend/xb_project/urls.py`
- Test: `backend/apps/logistics/tests/{test_api,test_permission}.py`

- [ ] Step 1: 写失败测试（CRUD + 权限：admin/tracker 写、salesman 只读+范围、finance 只读、删除仅 admin）
- [ ] Step 2: 验证失败
- [ ] Step 3: 实现 permissions.py + views.py（get_queryset 角色过滤 + perform_create 设 seq）+ urls.py + 挂载
- [ ] Step 4: 运行测试通过 + check 0 issues
- [ ] Step 5: Commit

---

### Task 4: 前端 api/路由/菜单

- Create: `frontend/src/api/logistics.ts`
- Modify: `frontend/src/router/index.ts`、`frontend/src/layouts/{MainLayout,MobileLayout}.vue`
- Test: `frontend/src/tests/logistics.test.ts`

- [ ] Step 1: 写失败测试（API 导出验证）
- [ ] Step 2: 验证失败
- [ ] Step 3: 实现 api/logistics.ts + 路由（PC /logistics + 移动 /m/logistics Placeholder）+ MainLayout 子菜单 + MobileLayout tabbar
- [ ] Step 4: 运行测试通过 + build 成功
- [ ] Step 5: Commit

---

### Task 5: PC 物流列表 + 表单 + OrderDetail 物流区块

- Create: `frontend/src/views/logistics/{LogisticsList,LogisticsForm}.vue`
- Modify: `frontend/src/views/orders/OrderDetail.vue`、`frontend/src/router/index.ts`

- [ ] Step 1: 实现 LogisticsList.vue（el-table + 筛选 order/payer/currency + 分页）
- [ ] Step 2: 实现 LogisticsForm.vue（新增/编辑：国内承运商下拉 type=domestic + 国际物流下拉 type=international + 单号 + 费用 + 币种 + 归属 + 备注）
- [ ] Step 3: OrderDetail.vue 加"物流发货"区块（每次发货信息 + 登记发货按钮 tracker/admin → 跳转表单）
- [ ] Step 4: 路由替换 Placeholder
- [ ] Step 5: vitest 全绿 + build 成功
- [ ] Step 6: Commit

---

### Task 6: 移动端物流列表 + 表单

- Create: `frontend/src/views/m/logistics/{MLogisticsList,MLogisticsForm}.vue`
- Modify: `frontend/src/router/index.ts`

- [ ] Step 1: 实现 MLogisticsList.vue（van-list + 筛选）
- [ ] Step 2: 实现 MLogisticsForm.vue（van-form 完整功能）
- [ ] Step 3: 路由替换 Placeholder
- [ ] Step 4: vitest 全绿 + build 成功
- [ ] Step 5: Commit

---

### Task 7: 集成验证 + neat-freak

- [ ] Step 1: 后端全量 pytest 全绿
- [ ] Step 2: 前端全量 vitest + build 成功
- [ ] Step 3: 本地联调（PC + 移动端物流 CRUD + OrderDetail 区块）
- [ ] Step 4: final whole-branch review (fable) + fix (sonnet) + re-review (fable)
- [ ] Step 5: neat-freak 整理
- [ ] Step 6: 合并 feature/logistics → dev