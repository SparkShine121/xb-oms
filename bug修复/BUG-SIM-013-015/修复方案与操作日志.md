# BUG-SIM-013 + 015 + 027 修复方案与操作日志 · 取消语义批次

- 日期：2026-09-20 ｜ 基线：dev `43e783f` ｜ 分支：`feature/bug-sim-013-015-cancel-semantics`
- 决策（grilling 确认）：Q1:a 跟单流转是唯一状态入口（admin 留纠错口但禁经编辑置"已取消"）；Q2:a 取消单冻结新增财务动作、存量结算可付款；Q3:a 工厂汇总排除取消单、对账单保留全部；Q4:a 订单列表加取消按钮（仅 admin）

## 实现

1. **013**：`OrderSerializer.validate`——任何路径 `tracking_status='已取消'` → 400"订单取消请使用「取消订单」操作"；非 admin 编辑忽略 tracking_status 字段；前端 PC/移动端状态选项移除"已取消"
2. **015**：
   - `generate_by_order`：取消订单的明细逐条进 skipped（200，决策静默语义）
   - `FactoryPaymentSerializer.validate`（create）：明细属已取消订单 → 400"已取消订单不可新建结算"
   - 付款记录不加限制（对存量结算清偿合法）
3. **Q3:a**：`FactorySummaryView` qs 排除取消单（`order_item__order__is_cancelled=False`）；`statement` 对账单不动（回归锁锁定）
4. **027**：`OrderList.vue` 操作列加"取消"按钮（`isAdmin` 且未取消；二次确认 → PATCH is_cancelled=true → reload）

## 测试（TDD 先红）

- 013：salesman PATCH 改状态被忽略（状态不变）/admin PATCH 置"已取消" 400/admin 纠错改其他状态 200
- 015：取消单 generate 全 skipped/手动建结算 400/存量结算付款 201（回归锁）
- Q3：factory-summary 不含取消单结算/statement 含取消单（锁定）
- 前端：build + vitest（无新增 UI 测试，状态选项与按钮为模板改动）
