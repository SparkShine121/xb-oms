# BUG-SIM-001 修复操作日志

- 日期：2026-09-11 ｜ 分支：`feature/bug-sim-001-bulk-delete`（自 dev `a0e1d66` 拉出）
- 方案：同目录《修复方案.md》（Q1:A Q2:a Q3:ii Q4:ii，grilling 确认）

## 执行记录

| # | 步骤 | 命令/操作 | 结果 |
|---|---|---|---|
| 1 | 提交留档规则 | dev 上 commit `a0e1d66`（CLAUDE.md 新增 bug 修复留档规则） | ✔ |
| 2 | 拉分支 | `git checkout -b feature/bug-sim-001-bulk-delete dev` | ✔ |
| 3 | 写失败测试 | 向 6 个既有测试文件追加 14 条用例（orders×5 / factory_payment×3 / logistics×2 / finance×2 / basic_info×1 / accounts×1） | ✔ 提交 `aa92691` |
| 4 | 验证 RED | `.venv/bin/python -m pytest -k "bulk"` → **8 failed**（正是越权本体：非 admin 批量删返回 200、无 forbidden/not_found 清单）+ 6 passed（admin 正常路径与已安全端点的回归锁，符合预期） | ✔ |
| 5 | 修正测试自身错误 | logistics 的 `tracker_client` 夹具返回元组需解包（测试 bug，非 RED 语义问题） | ✔ |
| 6 | 实现修复 | 重写 `backend/common/views.py::bulk_delete`：action 临时视为 destroy 走完整权限链 + `transaction.atomic` + forbidden/not_found 清单 + 全部禁删时 403 | ✔ 提交 `1cee2d0` |
| 7 | 验证 GREEN | 触碰的 6 个测试文件 52 passed；全量 `python -m pytest -q` → **179 passed**（基线 165 + 新增 14，无回归） | ✔ |
| 8 | 前端 RED | 新建 `src/tests/useBulkDelete.test.ts`（2 用例：全部成功→success；部分跳过→warning 列明）→ 第 2 条失败（旧实现固定弹"已删除 3 条"） | ✔ |
| 9 | 前端 GREEN | `useBulkDelete.ts::handleBatchDelete` 读取 `{deleted, forbidden, not_found}`，部分成功改弹 warning | ✔ 提交 `2cf6b78` |
| 10 | 前端验证 | `npx vitest run` 17/17 绿；`npm run build`（vue-tsc）通过（chunk 体积告警为既有问题） | ✔ |
| 11 | 合并回 dev | `git checkout dev && git merge feature/bug-sim-001-bulk-delete` | ✔ |

## 验证证据

- 后端：`179 passed`（原 165 全绿无回归 + 新增 14 条 BUG-SIM-001 回归测试）
- 前端：`17 passed`（新增 2 条 composable 测试）+ `vue-tsc` 构建通过
- 覆盖端点矩阵：orders（salesman/tracker/finance/admin + 混合清单）、factory_payment ×2、logistics、finance、basic_info、accounts——非 admin 批量删除现在一律 403，行为与单条删除完全一致

## 环境说明

- 本机为 macOS：Python 用 `backend/.venv/bin/python`（CLAUDE.md 中的 Windows 路径在 Mac 上不适用，pytest/vitest/npm 均直接可用）

## 遗留与关联

- BUG-SIM-025（范围外 id 静默跳过）已随本次顺带修复——如需在 02-bug清单.md 中标记状态，待确认后更新
- 审批对象删除后 bulk-delete 的 403 不写操作日志（与单条删除语义一致，403 留痕仅限已认证请求的 middleware 行为，未改动）
