# BUG-SIM-006 + BUG-SIM-012 修复方案与操作日志 · 金额校验批次

- 日期：2026-09-17 ｜ 基线：dev `1bd5b4c` ｜ 分支：`feature/bug-sim-006-012-amount-validation`
- 两 bug 同机制（金额域校验缺失）同批修复，共用本目录
- 决策（grilling 确认）：Q1:a 严格全套（amount>0 + 累计超付拒绝，select_for_update 锁）；Q2:i 顺带结算单金额>0

## 问题对照

| 报告症状 | 本批对策 |
|---|---|
| F-03 付款 -100 → 已付变负、状态回退未结 | Record.amount > 0（serializer + DB 约束） |
| F-06 付 0 成功 | 同上 |
| F-04 超付 410>360 → "已结"+对账单未付为负 | 视图层累计校验：paid + 本次 ≤ amount_cny，超则 400；事务内 select_for_update 锁父结算单防并发超付；update 改大金额同样拦截 |
| FP-04 手动结算单金额任意可建 | amount_cny > 0（Q2:i 顺带） |
| F-07b 回款 -500 成功、流水显示正收入 | PaymentIn.amount_usd > 0 |
| F-02b rate=0 汇率使利润清零（012） | ExchangeRate.rate > 0 |

## 实现

1. **serializer 校验**（友好中文 400）：四个 validate_amount* / validate_rate
2. **DB CheckConstraint 兜底**（防绕过 serializer 的写入）：4 条约束 + makemigrations；存量数据已扫描（0 违规，可安全上约束）
3. **超付累计校验**（只能视图层做，无法用 CHECK 表达）：`FactoryPaymentRecordViewSet.perform_create/perform_update` 在 atomic 内锁父单后校验，支持 update 时排除自身旧值

## 测试（TDD 先红后绿，9 条新增）

factory_payment（6）：负数付款 400/付0 400/超付 400 且已付不变/恰好付满→已结（回归锁）/update 改大金额超付 400/结算单金额 0 与负 400
finance（2）：回款负数 400/回款 0 400
orders（1）：汇率 0 与负 400（正数路径由既有 test_exchange_rate_crud_admin 回归锁定）

## 边界与遗留

- 超付校验在应用层（CHECK 约束表达不了跨行累计），已用行锁收紧并发窗口；跨请求的极端竞态（同事务外直接改 paid_amount）不在防御范围
- 回款与订单的金额勾稽（回款累计 vs 订单金额）属 BUG-SIM-007 唯一性/一致性范畴，本批不涉及
- Django admin 后台写入受 DB 约束兜底（IntegrityError），非本批范围（API 才是主入口）

## 执行记录（2026-09-17）

| 步骤 | 结果 |
|---|---|
| RED：9 条测试（factory_payment×6 / finance×2 / orders×1） | ✔ 8 failed + 1 passed（恰好付满回归锁） |
| serializer 校验 ×4 + DB CheckConstraint ×4（condition=，3 个迁移） | ✔ commit 8b476e4 |
| 超付校验（perform_create/perform_update，select_for_update 锁父单，update 排除自身） | ✔ |
| 过程修正：批量替换误插空 class Meta（IndentationError）×2 当场修复；check= 弃用参数改 condition= 重新生成迁移，告警清零 | ✔ |
| 全量 210 passed，迁移应用 dev 库，push 1bd5b4c..8b476e4 | ✔ |

## 旁观者审查收口（pass-with-notes，commit d8da642）

- 已修①（medium）：PATCH 付款记录更换所属结算单 → 400（换父绕过超付校验并使旧父 paid_amount 失真）
- 已修②（low→当场收）：结算单改小金额低于已付 → 400（防静默"已结"绕过超付不变量）
- 已修③（low→当场收）：一键生成跳过成本为 0 的明细（防 0 元结算单 + 整批 IntegrityError 500）
- 补测试 3 条（含 generate 路由修正：`orders/<id>/generate/`）；全量 213 passed
- 未实施（low，预存在）：Record.delete() 路径未包 atomic+行锁（仅 admin 可删）；并发自动化测试未写（以行锁+代码审查覆盖）

## 验证证据

后端 213 passed（210 + 3 收口）；全量无告警；push 已同步 origin/dev。
