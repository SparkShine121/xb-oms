# BUG-SIM-007 修复方案与操作日志 · 三处唯一性缺失

- 日期：2026-09-17 ｜ 基线：dev `9300927` ｜ 分支：`feature/bug-sim-007-uniqueness`（已合并回 dev）
- 决策（grilling 确认）：Q1:a 客户名全局唯一；Q2:a 回款 (订单，期数) 唯一；Q3:a 汇率 (币种对，日期) 唯一

## 问题与对策

| 报告症状 | 对策 |
|---|---|
| F-20 同名客户可连建（导入按名匹配会歧义） | `Customer.name unique=True` + serializer 校验"已存在同名客户" |
| F-07/C-05 同订单同期回款重复（串行+并发都成功） | `UniqueConstraint(order, installment)` + 校验"该订单此期回款已登记" |
| C-06 同 (币种对，日期) 汇率重复（get_effective_rate 同日静默取 id 大者） | `UniqueConstraint(currency_pair, effective_date)` + 校验"已有汇率记录，请编辑原记录" |
| （工程默认）并发撞约束产生 IntegrityError 裸 500（016 同源） | `common/exceptions.py` 增 IntegrityError → 400 code=1001 统一映射 |

存量数据扫描：三类重复均为 0，约束安全上线。

## 测试（TDD：3 条 RED 重复用例 + 6 条回归锁/边界）

- 重复路径：同名客户 400 / 同订单同期回款 400 / 同币种对同日期 400
- 边界：更新自身不误判、不同期放行、他单同期放行、不同日期/不同币种对放行、导入 _match_customer 不受影响
- 回归锁：DB 约束直写拦截（IntegrityError）、IntegrityError→400 单测

## 过程修正

- 两条新测试与 finance `setup` 夹具冲突（夹具已占两订单第 1 期）→ 改用空闲期数
- 既有 `test_finance_can_write_not_delete` 默认落第 1 期被新约束正确拒绝 → 改用第 2 期（测试意图不变，行为变化符合新规格）

## 验证证据

后端 **222 passed**（213 + 9）；迁移 3 个（basic_info 0007 / orders 0006 / finance 0003）已应用 dev 库。
