# BUG-SIM-011 + 017 修复方案与操作日志 · 毛利折算与金额联动（轻微项批次）

- 日期：2026-09-22 ｜ 基线：dev `6787886` ｜ 分支：`feature/bug-sim-011-017-profit-amount`
- 决策（grilling 确认）：Q1:a 无适用汇率回退全库最近一条；Q2:a 前端编辑页联动回填（后端/导入语义不动）

## 实现

1. **011**：`calc_order_profit`——订单日期无适用汇率（早于最早汇率）→ 回退全库最近一条汇率近似折算（1000 元单利润 280→900）；库中完全无汇率时维持 1:1 兜底（无可参照值）
2. **017**：`OrderForm.vue`——明细数量/单价变化与删行 → `amount_usd` 自动回填 Σ 小计（输入框仍可手改）；仅交互层联动，后端/导入的订单金额权威语义不变

## 测试

- 011：回退最近汇率（900，先红）+ 完全无汇率维持 1:1（回归锁）
- 017：交互层联动无组件测试基建（ARM jsdom 慢），以 vitest 全量 + build + 代码审查验证（与 UI 批次同口径）

## 验证证据

后端 **259 passed**（257+2）；前端 17 passed + vue-tsc 构建通过。
