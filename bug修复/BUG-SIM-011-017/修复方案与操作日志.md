# BUG-SIM-011 + 017 修复方案与操作日志 · 毛利折算与金额联动（轻微项批次）

- 日期：2026-09-22 ｜ 基线：dev `6787886` ｜ 分支：`feature/bug-sim-011-017-profit-amount`
- 决策（grilling 确认）：Q1:a 无适用汇率回退全库最近一条；Q2:a 前端编辑页联动回填（后端/导入语义不动）

## 实现

1. **011**：`calc_order_profit`——订单日期无适用汇率（早于最早汇率）→ 回退全库最近一条汇率近似折算（1000 元单利润 280→900）；库中完全无汇率时维持 1:1 兜底（无可参照值）
2. **017**：`OrderForm.vue`——明细数量/单价变化与删行 → `amount_usd` 自动回填 Σ 小计（输入框仍可手改）；仅交互层联动，后端/导入的订单金额权威语义不变

## 测试

- 011：回退最近汇率（900，先红）+ 完全无汇率维持 1:1（回归锁）
- 017：交互层联动无组件测试基建（ARM jsdom 慢），以 vitest 全量 + build + 代码审查验证（与 UI 批次同口径）

## 验证证据

后端 **259 passed**（257+2）；前端 17 passed + vue-tsc 构建通过。

## 旁观者审查收口（pass-with-notes，followup commit）

- 已修①（medium）：移动端 MOrderForm 补同款金额联动（017 原报告即两端问题）
- 已修②（low）：小计直改也联动（PC 小计输入框 @change；移动端小计为 readonly 无需）
- 已修③（low）：get_effective_rate 与回退查询加 `currency_pair='USD/CNY'` 过滤（防其他币对记录被毛利折算误取；该 classmethod 仅毛利计算一处消费）
- 已修④（low）：补 order_date=None 多条汇率取最近用例 + 非 USD/CNY 过滤用例（共 +2）
- 已记录（low，超出本批范围）：ExchangeRateViewSet 无重算钩子——补录历史汇率后既有订单毛利不刷新（曾按 1:1 算过的单不会自愈），待办"汇率变更后批量重算（admin 动作/管理命令）"
- 已记录（口径边界）：联动为显式触发（数量/单价/小计直改/删行），loadOrder 载入不触发——"订单金额≠明细和"的导入单载入后若编辑明细，Σ 会无提示覆写权威金额（属 Q2:a 决策内行为，前端已注明可手改）
- 实现备注：曾试用深度 watch 统一触发，因 loadOrder 载入会覆写导入金额语义而改回显式触发点

**验证证据（最终）**：后端 **261 passed**（259+2）；前端 17 passed + vue-tsc 构建通过；dev/test 同步推送。
