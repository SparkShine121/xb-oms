# BUG-SIM-003 修复方案与操作日志 · 导入更新绕过归属、状态机与审批

- 日期：2026-09-15 ｜ 基线：dev `6930278` ｜ 分支：`feature/bug-sim-003-import-status`（已合并回 dev，commit `384f20a`）
- 本 bug 的三个洞分两批关闭，审批洞经 grilling（Q2:a）确认按设计豁免

## 三洞处置总览（grilling 决策记录）

| 洞 | 处置 | 时间与位置 |
|---|---|---|
| ① 跨归属越权覆写 | **修复**：导入更新校验操作者数据范围（admin/finance 全部、salesman 自己客户、tracker 派给自己），范围外整单失败"无权限更新该订单" | 2026-09-15 随 BUG-SIM-002 修复（Q7:i），`importers._in_data_scope` |
| ② 状态机旁路（Excel 覆写 tracking_status / is_cancelled） | **修复（Q1:a）**：更新路径完全不覆写状态三件套 `ali_status`/`tracking_status`/`is_cancelled`——Excel 阿里状态快照仅新建建档时映射；跟单状态推进属 8 节点状态机职权、取消是显式动作 | 本次 commit `384f20a`，`importers` 主循环 defaults 剥离 |
| ③ 已批准订单更新不重挂审 | **按设计豁免（Q2:a）**：UI 编辑已批准订单本就"放行不挂审"（`system_mgmt/approval.py::resubmit_on_update` 的"标记不阻断"方案，docstring 明示），导入与 UI 同规则即一致性原则；真要收紧审批应 UI+导入一起，属 BUG-SIM-005/024 完整方案 | 无代码改动，清单标注关闭 |

## 实现（仅 ② 有代码改动）

`orders/importers.py` 主循环：构建 defaults 后，若订单已存在则 pop 掉 `ali_status`/`tracking_status`/`is_cancelled` 三个键再 `update_or_create`；新建订单行为完全不变。业务字段（金额/运费/备注/联系人/明细 diff）同步不受影响。

## 测试（TDD 先红后绿）

新增 3 条：
- `test_reimport_does_not_overwrite_tracking_status`：已推进到"质检"的单重导旧文件不被拉回"接单"，ali_status 不动
- `test_reimport_does_not_cancel_or_revive`：已取消单重导不被复活；正常单遇"交易失败"重导不被标记取消；新建路径映射照常（O2 对照）
- `test_reimport_still_updates_business_fields`：状态剥离不影响业务字段同步（回归锁）

修正 1 条旧测试：`test_import_upsert_replaces_items` 原断言"重导覆写 tracking_status='排产'"——该断言即本 bug 本体，按新规格改为断言状态不变（金额/明细断言保留）。

**验证**：后端全量 **200 passed**（197 + 3 新增，无回归）。

## 边界与遗留

- `ali_status` 在更新路径不再刷新——长期重导的老单其阿里原始状态会停留在建档值；属 Q1:a 语义的已知取舍（状态以系统内状态机为准）
- 审批收紧（已批准单编辑挂审）作为 BUG-SIM-005/024 的完整方案另行设计，UI 与导入须同批收紧
