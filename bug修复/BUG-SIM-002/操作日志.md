# BUG-SIM-002 修复操作日志

- 日期：2026-09-15 ｜ 分支：`feature/bug-sim-002-settlement-guard`（自 dev `de60ae5` 拉出，已合并回 dev）
- 方案：同目录《修复方案.md》（Q1:A Q2:i Q3:a Q4:i Q5:i Q6:b Q7:i，grilling 确认）

## 执行记录

| # | 步骤 | 结果 |
|---|---|---|
| 1 | 拉分支 `feature/bug-sim-002-settlement-guard` | ✔ |
| 2 | RED：向 test_serializer / test_api / test_importer 追加 14 条用例并跑红 | ✔ 12 failed（bug 本体：整组替换清结算、无冻结、无归属校验、无删除保护）+ 2 passed（现状已正确行为的回归锁：删无结算行、删无结算订单） |
| 3 | 提交红测试 `6e6084b` | ✔ |
| 4 | 实现 `orders/services.py`（check_items_diff / ensure_order_deletable / get_settled_item_ids） | ✔ |
| 5 | `OrderSerializer.update` 改 diff（带 id 原位更新/无 id 新建/缺失删除，校验前置+atomic 零写入）；OrderItemSerializer 的 id 改可写；create 时剔除 payload id | ✔ |
| 6 | importers 重写：`import_orders(file, user=None)`；每订单 `transaction.atomic`；新建走 `_create_items`（行为不变）、更新走 `_update_items`（product_no 匹配 diff + 冻结 + 删除保护 + 重复/空编号安全失败 + `_in_data_scope` 归属校验 Q7:i） | ✔ |
| 7 | `OrderViewSet.perform_destroy` 删除守卫；`BaseModelViewSet.validate_bulk_delete` 钩子 + bulk 循环接入，冲突对象进 forbidden 清单 | ✔ |
| 8 | GREEN：orders 三测试文件 29 passed；全量后端 **193 passed**（179 + 新 14，无回归） | ✔ |
| 9 | 前端 OrderForm 加载/提交均带 `id`；vitest 17/17、`npm run build`（vue-tsc）通过 | ✔ 提交 `1f7a73c` |
| 10 | 合并回 dev + 方案归档 `a273246` | ✔ |

## 验证证据

- 后端：193 passed（含 14 条 BUG-SIM-002 回归：diff 保 id、删结算行 400、金额冻结、状态改动零删行、导入保结算、缺行/改价/越权/重复键/空键导入失败、挂结算订单单删 400、批量删 forbidden）
- 前端：17 passed + vue-tsc 构建通过

## 遗留与关联

- **BUG-SIM-003 仍开放**（按 Q7:i 范围决定）：导入更新仍会覆写 tracking_status（状态机旁路）、已批准订单不重挂审；跨归属越权已由 `_in_data_scope` 封堵
- `validate_bulk_delete` 钩子的冲突原因字符串目前仅用于判定，响应 forbidden 仍为 id 清单（保持 BUG-SIM-001 契约）；如需前端展示原因可后续扩展
- Excel 重导的"本月更新上月订单"场景已可用：未动结算的行原位更新（行 id 不变）、spec/model 可改、改价/删结算行被整单拒绝并给出行号原因

## 旁观者审查收口（2026-09-15，commit 226461a）

- 已修：导入更新遇 DB 侧空 product_no 明细行 → 整单拒绝（原先会被误判"Excel 缺席"而静默删除）
- 已修：`check_items_diff` 金额字段为 None 视为未提交（服务层防御；API 层 null 由 DRF 字段校验先行 400，已锁定该语义）
- 已补测试：bogus id 400 回归锁、null 字段校验、`check_items_diff` 直测
- **API 契约备忘（未改行为）**：diff 语义是"payload 缺席的行=删除"。当前前端每次提交全量 items 所以安全；若未来出现只提交 items 子集的客户端，未列出的非结算行会被删除（结算行有保护）。第三方接入需遵守"PATCH 必须提交全量 items"契约
- 审查 low 项未实施：bulk_delete 对 validate_bulk_delete 抛异常的统一兜底（当前 OrderViewSet 已自行 try/except，约定子类照做）；update 与删除间的并发窗口（SQLite/内部系统低并发下可接受，如需收紧可加 select_for_update）
